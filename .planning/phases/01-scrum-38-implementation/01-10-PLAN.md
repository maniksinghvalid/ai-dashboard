---
phase: 01-scrum-38-implementation
plan: 10
type: execute
wave: 3
depends_on: ["01-03", "01-07", "01-08"]
files_modified:
  - arch/sentiment-widget.html
  - lib/hooks/use-dashboard.ts
  - components/widgets/SentimentWidget.tsx
  - components/widgets/HeroStoryCard.tsx
  - components/DashboardShell.tsx
autonomous: true
requirements:
  - REQ-dashboard-data-wiring
  - REQ-sentiment-widget-polish
must_haves:
  truths:
    - "`arch/sentiment-widget.html` exists as the gate-2 HTML mockup (ROADMAP SC5 + REQ-sentiment-widget-polish acceptance — fixes prior-iteration WARNING 3)"
    - "`useDashboard()` returns two NEW data legs: `sentiment` (Sentiment) and `hero` (HeroStory), both via `useApiData<T>()` — no direct `useSWR` calls (CON-swr-wrapper)"
    - "`SentimentWidget` accepts `{ data, stale, isLoading, error }` (widget contract); zero hardcoded percentages remain"
    - "`HeroStoryCard` renders the API payload (`apiHero` prop) when present; falls back to existing `deriveHeroStory()` on 503 / loading / null (Hero-card flicker policy — never blank)"
    - "`DashboardShell` destructures the two new legs and passes them through; the existing `WidgetErrorBoundary` wrap remains (7 widgets — structurally asserted via `fallbackTitle` count)"
    - "Three sentiment percentages visibly sum to 100 (acceptance criterion 1)"
  artifacts:
    - path: "arch/sentiment-widget.html"
      provides: "Gate-2 static HTML mockup of the sentiment widget (no React, no JS framework)"
      contains: "sampleSize"
    - path: "lib/hooks/use-dashboard.ts"
      provides: "Extended hook with sentiment + hero legs"
      exports: ["useDashboard"]
    - path: "components/widgets/SentimentWidget.tsx"
      provides: "Live data widget conforming to {data, stale, isLoading, error} contract"
      contains: "isLoading"
    - path: "components/widgets/HeroStoryCard.tsx"
      provides: "API-first hero with client-derive fallback"
      contains: "apiHero"
    - path: "components/DashboardShell.tsx"
      provides: "Updated shell passing new data through"
      contains: "sentiment.data"
  key_links:
    - from: "lib/hooks/use-dashboard.ts"
      to: "/api/sentiment + /api/hero"
      via: "useApiData<Sentiment>('/api/sentiment') + useApiData<HeroStory>('/api/hero')"
      pattern: "useApiData<(Sentiment|HeroStory)>"
    - from: "components/DashboardShell.tsx"
      to: "SentimentWidget + HeroStoryCard"
      via: "data + apiHero props"
      pattern: "data=\\{sentiment\\.data\\}"
---

<objective>
Ship the gate-2 sentiment HTML mockup, wire the new `/api/sentiment` and `/api/hero` endpoints into the dashboard (REQ-dashboard-data-wiring), and polish the `SentimentWidget` to consume live data via the standard widget contract (REQ-sentiment-widget-polish). Also adds the API-first hero rendering with `deriveHeroStory()` fallback in `HeroStoryCard.tsx` (per "Hero-card flicker policy" in CONTEXT). All five files share the user-facing concern (dashboard reads new data + the design reference that justifies the visual choices), so they ship together — `DashboardShell.tsx` is the unifying integration point.

Purpose: Closes the user-facing loop. Without this plan, the data exists in caches and routes but never renders. Adding `arch/sentiment-widget.html` first fixes prior-iteration WARNING 3: ROADMAP Phase 1 SC5 explicitly requires "the gate-2 HTML mockup is committed under `arch/`", and REQ-sentiment-widget-polish acceptance has the same clause. The mockup committed BEFORE the React refactor anchors the visual contract.
Output: New `arch/sentiment-widget.html`, extended hook, refactored `SentimentWidget`, API-first `HeroStoryCard`, updated `DashboardShell`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-CONTEXT.md
@/Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-PATTERNS.md
@/Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/intel/constraints.md
@/Users/akshaydhaliwal/claude-code/ai-dashboard/CLAUDE.md
@/Users/akshaydhaliwal/claude-code/ai-dashboard/lib/hooks/use-dashboard.ts
@/Users/akshaydhaliwal/claude-code/ai-dashboard/lib/hooks/use-api-data.ts
@/Users/akshaydhaliwal/claude-code/ai-dashboard/components/widgets/SentimentWidget.tsx
@/Users/akshaydhaliwal/claude-code/ai-dashboard/components/widgets/HeroStoryCard.tsx
@/Users/akshaydhaliwal/claude-code/ai-dashboard/components/widgets/NewsWidget.tsx
@/Users/akshaydhaliwal/claude-code/ai-dashboard/components/DashboardShell.tsx
@/Users/akshaydhaliwal/claude-code/ai-dashboard/lib/types.ts
@/Users/akshaydhaliwal/claude-code/ai-dashboard/arch/ai_pulse_dashboard.html

<interfaces>
From lib/types.ts (plan 03):
  export interface Sentiment { positive: number; neutral: number; negative: number; sampleSize: number; }
  export interface HeroStory extends TrendingTopic { headline; thumbnailUrl; link; }

From lib/hooks/use-api-data.ts (existing):
  export function useApiData<T>(endpoint: string, refreshInterval = 60_000): { data: T|null; cachedAt; stale; isLoading; error };

Widget contract (canonical example — components/widgets/NewsWidget.tsx:54-64 + 75-92):
  { items|data: T|null; stale: boolean; isLoading: boolean; error: Error|null }
  Render via WidgetCard, handle isLoading → WidgetSkeleton; error → "Failed to load..."; empty → "No data..."; data → render.

Existing deriveHeroStory (HeroStoryCard.tsx:8-34) — KEEP as fallback.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create arch/sentiment-widget.html — gate-2 static HTML mockup</name>
  <read_first>
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/arch/ai_pulse_dashboard.html (whole-dashboard design reference — the sentiment-widget visual style must mirror its dark theme + accent palette; copy the relevant tokens and section styling)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/REQUIREMENTS.md (REQ-sentiment-widget-polish acceptance: "HTML mockup committed under `arch/` (gate 2 output) before implementation")
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/ROADMAP.md (Phase 1 SC5: "the gate-2 HTML mockup is committed under `arch/`")
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/CLAUDE.md ("Dark theme" section — CSS variable names: `--surface`, `--surface-2`, `--accent` `#7c6eff`, `--accent-secondary` `#c084fc`, `--muted`, `--border`. Font vars: `--font-dm-sans`, `--font-space-mono`. Color tokens: `bg-green` / `bg-amber` / `bg-red` for sentiment segments.)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/components/widgets/SentimentWidget.tsx (current placeholder — the visual treatment to replicate in HTML form before the React refactor)
  </read_first>
  <acceptance_criteria>
    - `arch/sentiment-widget.html` exists at the project root under `arch/`.
    - The file is self-contained static HTML (no React, no JS framework, no external script bundles). Inline `<style>` block is acceptable; a referenced stylesheet from elsewhere in `arch/` is not.
    - Renders the four data labels: "Positive", "Neutral", "Negative", and a sample-size indicator (e.g., "n=184").
    - Uses dummy data: positive=62, neutral=28, negative=10, sampleSize=184. The three sentiment percentages sum to 100.
    - Uses at least 3 of the named CSS variables from the project's dark theme: `--surface-2`, `--accent`, `--accent-secondary`, `--muted`, `--border`, `--font-space-mono` (any 3 or more).
    - Mirrors the visual style of `arch/ai_pulse_dashboard.html` (dark background, accent purple, monospace labels).
  </acceptance_criteria>
  <action>
    Create `arch/sentiment-widget.html`. Structure: a single full HTML document (`<!DOCTYPE html>`, `<html lang="en">`, `<head>` with `<meta charset="utf-8">`, a `<title>`, and a `<style>` block; `<body>` containing one widget card).

    Inside the `<style>` block, declare the dark-theme tokens as CSS custom properties on `:root` (mirror the values from CLAUDE.md / `globals.css`): at least `--surface-2: #13132b`, `--accent: #7c6eff`, `--accent-secondary: #c084fc`, `--muted: #6b668a`, `--border: rgba(124, 110, 255, 0.18)`. Use these tokens throughout the widget styles — declare a `body { background: #0d0d1f; color: #e7e5ea; font-family: 'DM Sans', system-ui, sans-serif; }` rule.

    Build the widget body as a card with:
    - A header row containing a small accent-tinted icon circle (rendered as a `<span>` with `background: var(--accent)`), the title "Community Sentiment" in body font, and a monospace badge "n=184" on the right (use `font-family: 'Space Mono', monospace` or fall back to the system mono stack; the badge should sit at `color: var(--muted)`).
    - A 3-segment horizontal bar (single flex row, height 8-12px, rounded). Segments: 62% with `background: #22c55e` (the project's `bg-green`), 28% with `background: #f59e0b` (the project's `bg-amber`), 10% with `background: #ef4444` (the project's `bg-red`). Inline `style="flex: 62"` / `flex: 28` / `flex: 10` is the simplest way to drive widths.
    - A legend with three rows below the bar. Each row: a 10px colored swatch (matching the bar segment color), a label ("Positive" / "Neutral" / "Negative"), and a right-aligned percentage value ("62%" / "28%" / "10%") in monospace. Use `var(--muted)` for the label text and the default body color for the percentage value.
    - All four data labels MUST be present as literal text in the rendered HTML (`Positive`, `Neutral`, `Negative`, and a sample-size string containing `184`).

    Keep the markup minimal — under ~80 lines total. Do NOT add JavaScript. Do NOT depend on Tailwind classes (this is a static mockup; the React refactor in task 3 uses Tailwind). Do NOT depend on any external stylesheet or webfont; specify the webfonts as a `@font-face`-free Google Fonts `<link>` only if needed (preferred: rely on system font fallback — `font-family: 'DM Sans', system-ui, sans-serif`).
  </action>
  <verify>
    <automated>cd /Users/akshaydhaliwal/claude-code/ai-dashboard && test -f arch/sentiment-widget.html && grep -q 'Positive' arch/sentiment-widget.html && grep -q 'Neutral' arch/sentiment-widget.html && grep -q 'Negative' arch/sentiment-widget.html && grep -q '184' arch/sentiment-widget.html && [ "$(grep -cE -- '--(surface-2|accent|accent-secondary|muted|border|font-space-mono)' arch/sentiment-widget.html)" -ge 3 ] && ! grep -q '<script' arch/sentiment-widget.html</automated>
  </verify>
  <done>
    `arch/sentiment-widget.html` exists. Contains all four data labels. Uses at least 3 named CSS variables. No `<script>` tag. Renders standalone in a browser.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend useDashboard hook with sentiment + hero legs</name>
  <read_first>
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/hooks/use-dashboard.ts (current hook — exact shape to extend; lines 12-19)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/hooks/use-api-data.ts (the wrapper to reuse — CON-swr-wrapper)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/types.ts (plan 03 — `Sentiment` and `HeroStory` types)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-CONTEXT.md (REQ-dashboard-data-wiring acceptance: must use `useApiData`, never `useSWR`)
  </read_first>
  <acceptance_criteria>
    - `useDashboard()` returns `{ youtube, reddit, twitter, news, trending, sentiment, hero }`.
    - Both new legs use `useApiData<T>()`.
    - No `useSWR` import or call appears in the file.
  </acceptance_criteria>
  <action>
    Extend `lib/hooks/use-dashboard.ts`. Add to the type-imports block at top: `Sentiment` and `HeroStory` from `@/lib/types`. Inside `useDashboard()`, immediately after the existing `trending` line, add two new lines (mirror existing spacing/style): `const sentiment = useApiData<Sentiment>("/api/sentiment");` and `const hero = useApiData<HeroStory>("/api/hero");`. Extend the return statement to include both: `return { youtube, reddit, twitter, news, trending, sentiment, hero };`. Do NOT call `useSWR` directly (CON-swr-wrapper). Do NOT override the default `refreshInterval` — 60s is appropriate (sentiment cache is 15-min; hero cache is 10-min — both refresh signals come from the cron, not the SWR poll cadence).
  </action>
  <verify>
    <automated>cd /Users/akshaydhaliwal/claude-code/ai-dashboard && npx tsc --noEmit && grep -q 'useApiData<Sentiment>' lib/hooks/use-dashboard.ts && grep -q 'useApiData<HeroStory>' lib/hooks/use-dashboard.ts && grep -q 'sentiment, hero' lib/hooks/use-dashboard.ts && ! grep 'useSWR' lib/hooks/use-dashboard.ts</automated>
  </verify>
  <done>
    Hook returns `{ youtube, reddit, twitter, news, trending, sentiment, hero }`. Both new legs use `useApiData`. No direct `useSWR` calls.
  </done>
</task>

<task type="auto">
  <name>Task 3: Refactor SentimentWidget to consume live data via widget contract</name>
  <read_first>
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/components/widgets/SentimentWidget.tsx (current placeholder — lines 3-37; hardcoded 58/26/16 must go)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/components/widgets/NewsWidget.tsx (canonical sibling — props contract at 54-64; state ladder at 75-92; `WidgetCard` usage at 67-74)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/components/widgets/WidgetCard.tsx (the wrapper component — read for prop expectations)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/components/widgets/WidgetSkeleton.tsx (the loading skeleton component)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/types.ts (`Sentiment` interface)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-CONTEXT.md (REQ-sentiment-widget-polish acceptance: no hardcoded sample data; live percentages sum to 100; match `--surface-2` / `--accent` / `--accent-secondary` dark theme tokens; existing `bg-green` / `bg-amber` / `bg-red` are correct)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/arch/sentiment-widget.html (the gate-2 mockup from task 1 — visual reference for the refactor)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/arch/ai_pulse_dashboard.html (whole-dashboard design reference — existing dark theme palette to match)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/CLAUDE.md ("Tailwind color overrides" note: `green`/`red`/`amber` only have DEFAULT/400/500 — do NOT use `green-100` etc.)
  </read_first>
  <acceptance_criteria>
    - Widget signature: `({ data, stale, isLoading, error }: { data: Sentiment | null; stale: boolean; isLoading: boolean; error: Error | null })`.
    - No hardcoded 58/26/16 percentages anywhere; no "Sample data" string.
    - Bar widths driven by `data.positive`, `data.neutral`, `data.negative`.
    - Loading/error/empty/data states all handled.
    - No `useSWR` / `useApiData` / `useDashboard` import inside this file.
  </acceptance_criteria>
  <action>
    Rewrite `components/widgets/SentimentWidget.tsx`. New signature: `export function SentimentWidget({ data, stale, isLoading, error }: { data: Sentiment | null; stale: boolean; isLoading: boolean; error: Error | null })`. Import `Sentiment` from `@/lib/types`. Import `WidgetCard` and `WidgetSkeleton` as the existing sibling widgets do.

    Render via `WidgetCard` with: `icon="😐"`, `iconBg="var(--amber)"` (or replace with `var(--accent)` for visual polish — Claude's discretion; existing dashboard uses accent purple for the sentiment widget per arch reference), `title="Community Sentiment"`, `stale={stale}`, `badge={data?.sampleSize ? \`n=${data.sampleSize}\` : undefined}` (REPLACE the existing "Sample data" badge — REQ-sentiment-widget-polish forbids hardcoded sample data).

    Implement the state ladder (mirror NewsWidget.tsx:75-92):
    (a) `isLoading` → `<WidgetSkeleton lines={3} />`
    (b) `error` → `<p className="py-6 text-center text-[11px] text-muted">Failed to load — retrying…</p>`
    (c) `!data || data.sampleSize === 0` → `<p className="py-6 text-center text-[11px] text-muted">No sentiment data yet</p>`
    (d) Otherwise render the 3-segment bar (visual treatment to mirror `arch/sentiment-widget.html` from task 1) with widths driven by `data.positive`, `data.neutral`, `data.negative` (not 58/26/16). The legend rows should show the same three percentages and the labels "Positive", "Neutral", "Negative". Continue using `bg-green` / `bg-amber` / `bg-red` (existing Tailwind tokens — DEFAULT shades only per CLAUDE.md warning; no `green-100`).

    Do NOT hardcode any percentage value. Do NOT introduce a "Sample data" badge. Do NOT import `useSWR`, `useApiData`, or `useDashboard` here — widgets receive props (widget contract).
  </action>
  <verify>
    <automated>cd /Users/akshaydhaliwal/claude-code/ai-dashboard && npx tsc --noEmit && ! grep -E '"58%"|"26%"|"16%"|Sample data' components/widgets/SentimentWidget.tsx && grep -q 'data: Sentiment | null' components/widgets/SentimentWidget.tsx && grep -q 'isLoading' components/widgets/SentimentWidget.tsx && grep -q 'data.positive' components/widgets/SentimentWidget.tsx && grep -q 'data.neutral' components/widgets/SentimentWidget.tsx && grep -q 'data.negative' components/widgets/SentimentWidget.tsx && ! grep -q 'useSWR\|useApiData' components/widgets/SentimentWidget.tsx</automated>
  </verify>
  <done>
    No hardcoded 58/26/16 or "Sample data" string. Widget accepts `{ data, stale, isLoading, error }`. Loading/error/empty/data states all handled. Three percentages from `data.positive`/`data.neutral`/`data.negative` drive the bar widths.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add API-first rendering with deriveHeroStory fallback in HeroStoryCard</name>
  <read_first>
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/components/widgets/HeroStoryCard.tsx (full file — `deriveHeroStory` at 8-34 KEEP; existing props at 56-68 EXTEND; render JSX at 97-158 REUSE)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/types.ts (`HeroStory`)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-CONTEXT.md ("Hero-card flicker policy": on first paint and on 503, fall back to client-side `deriveHeroStory()`. NEVER blank the card during fetch.)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-PATTERNS.md ("components/widgets/HeroStoryCard.tsx" section — render priority)
  </read_first>
  <acceptance_criteria>
    - `apiHero?: HeroStory | null` prop added to the component signature; existing props unchanged.
    - Render priority: `apiHero` → `deriveHeroStory(...)` → loading skeleton (only if both yield null AND isLoading).
    - `deriveHeroStory` remains exported (single export).
  </acceptance_criteria>
  <action>
    Extend `components/widgets/HeroStoryCard.tsx`. KEEP the existing exported `deriveHeroStory(topics, videos, news)` function unchanged (lines 8-34 — used as fallback and as an export for any other consumer).

    Modify the `HeroStoryCard` component signature to add a new optional prop: `apiHero?: HeroStory | null` (driven by `useDashboard().hero.data`). The full destructured props now: `{ topics, videos, news, isLoading, stale = false, apiHero = null }`. Type annotation: add `apiHero?: HeroStory | null`.

    Inside the component, AT THE TOP of the render logic (BEFORE the existing `if (isLoading)` block), compute the hero source with this priority (Hero-card flicker policy):
    (a) If `apiHero` is truthy → use `apiHero` as the hero.
    (b) Else if `isLoading` AND the existing `deriveHeroStory(topics, videos, news)` returns non-null → use that (NEVER blank during first paint).
    (c) Else → use `deriveHeroStory(topics, videos, news)` (handles 503 / null apiHero / fetch error path).

    Implementation pattern: declare `const hero: HeroStory | null = apiHero ?? deriveHeroStory(topics, videos, news);` then RESERVE the existing `isLoading` skeleton path ONLY when both `hero === null` AND `isLoading === true` (so a stale `apiHero` or a successful derive renders immediately while a new fetch is in flight — preventing blanking).

    Reuse the existing return JSX (lines 97-158) verbatim — it already accepts a single `hero: HeroStory` and renders all fields. Do NOT duplicate the JSX. Do NOT introduce new React state hooks (Claude's discretion per CONTEXT line 113 — pure prop composition is fine).
  </action>
  <verify>
    <automated>cd /Users/akshaydhaliwal/claude-code/ai-dashboard && npx tsc --noEmit && grep -q 'apiHero' components/widgets/HeroStoryCard.tsx && grep -q 'apiHero\?: HeroStory' components/widgets/HeroStoryCard.tsx && grep -q 'deriveHeroStory' components/widgets/HeroStoryCard.tsx && grep -cE 'export function deriveHeroStory' components/widgets/HeroStoryCard.tsx | awk '$1 == 1 {exit 0} {exit 1}'</automated>
  </verify>
  <done>
    `apiHero` prop is the optional API-first source. `deriveHeroStory` remains exported and is the fallback. The card never blanks during loading when fallback data is available.
  </done>
</task>

<task type="auto">
  <name>Task 5: Update DashboardShell to destructure and pass through sentiment + hero</name>
  <read_first>
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/components/DashboardShell.tsx (full file — line 17 destructure; lines 48-69 HeroStoryCard + SentimentWidget wraps)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/hooks/use-dashboard.ts (extended in task 2 of this plan — now returns `sentiment` and `hero`)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/components/widgets/SentimentWidget.tsx (rewritten in task 3 — props `{ data, stale, isLoading, error }`)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/components/widgets/HeroStoryCard.tsx (extended in task 4 — accepts `apiHero` prop)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-CONTEXT.md (REQ-sentiment-widget-polish: widget wrapped in `WidgetErrorBoundary` via `DashboardShell` — preserve existing wraps)
  </read_first>
  <acceptance_criteria>
    - Shell destructures `sentiment` and `hero` from `useDashboard()`.
    - `<SentimentWidget>` receives the four contract props.
    - `<HeroStoryCard>` receives `apiHero={hero.data}` (in addition to existing props).
    - Exactly 7 `WidgetErrorBoundary fallbackTitle=` occurrences (one per widget — structural assertion, replaces the prior-iteration brittle ">= 14" tag count).
    - No widget reordering or removal of existing props.
  </acceptance_criteria>
  <action>
    Edit `components/DashboardShell.tsx`. Update the `useDashboard()` destructure (line 17) to: `const { youtube, reddit, twitter, news, trending, sentiment, hero } = useDashboard();`.

    Update the `<HeroStoryCard ... />` JSX block (currently lines 49-55) — ADD one new prop while preserving every existing prop: `apiHero={hero.data}`. Final props: `topics={trending.data} videos={youtube.data} news={news.data} isLoading={trending.isLoading} stale={trending.stale || youtube.stale || news.stale} apiHero={hero.data}`. The existing `WidgetErrorBoundary` wrap stays unchanged.

    Update the `<SentimentWidget />` JSX (currently line 68 — has no props). Replace the self-closing `<SentimentWidget />` with the four standard widget-contract props: `<SentimentWidget data={sentiment.data} stale={sentiment.stale} isLoading={sentiment.isLoading} error={sentiment.error} />`. The existing `WidgetErrorBoundary` wrap with `fallbackTitle="Sentiment unavailable"` (line 67) stays unchanged.

    Do NOT remove any `WidgetErrorBoundary`. Do NOT reorder widgets. Do NOT remove existing props on other widgets.
  </action>
  <verify>
    <automated>cd /Users/akshaydhaliwal/claude-code/ai-dashboard && npx tsc --noEmit && grep -q 'sentiment, hero' components/DashboardShell.tsx && grep -q 'data={sentiment.data}' components/DashboardShell.tsx && grep -q 'apiHero={hero.data}' components/DashboardShell.tsx && [ "$(grep -c 'WidgetErrorBoundary fallbackTitle' components/DashboardShell.tsx)" -eq 7 ]</automated>
  </verify>
  <done>
    Shell destructures `sentiment` and `hero` from `useDashboard()`. `SentimentWidget` receives the four contract props. `HeroStoryCard` receives `apiHero`. Exactly 7 `WidgetErrorBoundary fallbackTitle=` occurrences (one per widget — structural assertion). Project builds and lints clean.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean
- `npx next lint --quiet` clean
- `npx next build` succeeds (full integration check)
- `test -f arch/sentiment-widget.html` (the gate-2 mockup — fixes prior-iteration WARNING 3)
- `grep -c "useSWR" lib/hooks/use-dashboard.ts` == 0 (must use `useApiData`)
- `grep -c "Sample data\|58%\|26%\|16%" components/widgets/SentimentWidget.tsx` == 0
- `grep -c "WidgetErrorBoundary fallbackTitle" components/DashboardShell.tsx` == 7 (structural assertion replacing the prior brittle ">= 14" tag count — INFO 6)
- Manual: load dashboard in browser; `SentimentWidget` shows live percentages summing to 100 OR empty state ("No sentiment data yet") on cold start
- Manual: when `/api/hero` returns 503, the hero card still renders via `deriveHeroStory()` (never blank)
</verification>

<success_criteria>
- REQ-dashboard-data-wiring met: hook + shell wire both new endpoints via `useApiData`
- REQ-sentiment-widget-polish met: zero hardcoded percentages; widget contract honored; `WidgetErrorBoundary` wrap preserved; gate-2 HTML mockup committed under `arch/`
- Hero-card flicker policy met: API-first with `deriveHeroStory()` fallback; never blanks
- ROADMAP success criteria 2 and 5 closed (SC5 now satisfied by `arch/sentiment-widget.html`)
</success_criteria>

<output>
After completion, create `.planning/phases/01-scrum-38-implementation/01-10-SUMMARY.md` per @$HOME/.claude/get-shit-done/templates/summary.md.
</output>
