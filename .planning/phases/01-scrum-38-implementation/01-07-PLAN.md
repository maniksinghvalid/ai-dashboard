---
phase: 01-scrum-38-implementation
plan: 07
type: execute
wave: 2
depends_on: ["01-03"]
files_modified:
  - lib/api/sentiment.ts
  - lib/api/sentiment.test.ts
  - app/api/sentiment/route.ts
autonomous: true
requirements:
  - REQ-sentiment-engine
must_haves:
  truths:
    - "`GET /api/sentiment` returns `{positive, neutral, negative, sampleSize}` with three percentages summing to 100"
    - "Cold-miss returns 503 (no stale fallback — mirrors `/api/trending`)"
    - "Inputs are preprocessed per the cardiffnlp model card (F2): `@username` → `@user`, `http*` URL → `http`"
    - "Daily char-budget circuit breaker (`SENTIMENT_DAILY_CHAR_BUDGET`, default 200000) trips before quota overrun (D1, F1)"
    - "Provider is Together AI (D7); env var is `TOGETHER_API_KEY` (F1); `HUGGINGFACE_API_TOKEN` is NEVER referenced"
    - "On vendor 401, the client logs explicitly AND calls `Sentry.captureException` guarded by `NEXT_PUBLIC_SENTRY_DSN` (CON-key-rotation-observability)"
  artifacts:
    - path: "lib/api/sentiment.ts"
      provides: "Together AI batch sentiment + preprocessText + char-budget breaker + cache write"
      exports: ["preprocessText", "aggregateSentiment", "fetchAndCacheSentiment"]
    - path: "lib/api/sentiment.test.ts"
      provides: "preprocessText contract tests + aggregation tests"
      contains: "describe"
    - path: "app/api/sentiment/route.ts"
      provides: "Read-only cache endpoint mirroring /api/trending pattern"
      contains: "force-dynamic"
  key_links:
    - from: "lib/api/sentiment.ts"
      to: "Together AI inference API"
      via: "fetch with Bearer ${TOGETHER_API_KEY}"
      pattern: "Authorization.*Bearer.*TOGETHER_API_KEY"
    - from: "lib/api/sentiment.ts"
      to: "Redis budget counter"
      via: "getRedis().incrby for sentiment:budget:YYYY-MM-DD"
      pattern: "sentiment:budget"
    - from: "app/api/sentiment/route.ts"
      to: "CACHE_KEYS.sentiment"
      via: "cacheGet<Sentiment>"
      pattern: "cacheGet.*CACHE_KEYS\\.sentiment"
---

<objective>
Build the Sentiment Analysis Engine (D7 / REQ-sentiment-engine). Ships `lib/api/sentiment.ts` (preprocessText, char-budget circuit breaker, Together AI batch call, aggregation, cache write), paired Vitest, and the read-only `/api/sentiment` route. Engine is wired into cron Tier 3 by plan 09 — this plan does not modify the cron.

Purpose: Activates the "live cross-platform AI sentiment" goal pillar. Together AI is the locked provider (D1/D7), `TOGETHER_API_KEY` is the locked env var (F1), and `HUGGINGFACE_API_TOKEN` is explicitly forbidden.
Output: `lib/api/sentiment.ts`, `lib/api/sentiment.test.ts`, `app/api/sentiment/route.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-CONTEXT.md
@/Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-PATTERNS.md
@/Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/intel/constraints.md
@/Users/akshaydhaliwal/claude-code/ai-dashboard/CLAUDE.md
@/Users/akshaydhaliwal/claude-code/ai-dashboard/lib/api/twitter.ts
@/Users/akshaydhaliwal/claude-code/ai-dashboard/lib/cache/helpers.ts
@/Users/akshaydhaliwal/claude-code/ai-dashboard/lib/cache/redis.ts
@/Users/akshaydhaliwal/claude-code/ai-dashboard/app/api/trending/route.ts
@/Users/akshaydhaliwal/claude-code/ai-dashboard/lib/types.ts
@/Users/akshaydhaliwal/claude-code/ai-dashboard/lib/constants.ts

<interfaces>
From lib/types.ts (plan 03):
  export interface Sentiment { positive: number; neutral: number; negative: number; sampleSize: number; }

From lib/constants.ts (plan 03):
  CACHE_KEYS.sentiment = "sentiment:latest"  // 15-min TTL via CACHE_MAX_AGE.default

From lib/cache/helpers.ts:
  cacheSet<T>(key: string, data: T): Promise<void>
  cacheGet<T>(key: string, maxAgeMs: number): Promise<{ data: T; fetchedAt: string; stale: boolean } | null>

Bearer-token fetch pattern (lib/api/twitter.ts:37-50) — reuse shape.

Cardiffnlp model card requirement (F2):
  @username  →  @user  (literal)
  http*       →  http   (literal)

Together AI inference endpoint (Claude's discretion — typical):
  POST https://api.together.xyz/v1/chat/completions  (or text-classification endpoint per Together docs)
  Authorization: Bearer ${TOGETHER_API_KEY}
  Model: cardiffnlp/twitter-roberta-base-sentiment-latest
  Batch size: ≤ 100 per request
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create lib/api/sentiment.ts with preprocessText, char-budget breaker, batch fetch, aggregation, cache write</name>
  <read_first>
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/api/twitter.ts (Bearer-token fetch + env guard + `Promise.allSettled` patterns to mirror)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/cache/helpers.ts (`cacheSet` — final write)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/cache/redis.ts (`getRedis` — used for daily budget counter)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-CONTEXT.md (D1/D7/F1/F2/CON-key-rotation-observability — all locked)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/intel/constraints.md (CON-hf-quota-budget, CON-sentiment-5s-budget, CON-cardiffnlp-preprocessing)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-PATTERNS.md ("lib/api/sentiment.ts" section)
  </read_first>
  <behavior>
    - Test 1: `preprocessText("Hello @karpathy check https://x.com/foo")` returns `"Hello @user check http"`. (Both rules applied.)
    - Test 2: `preprocessText("plain text no replacements")` returns `"plain text no replacements"` unchanged.
    - Test 3: `aggregateSentiment([{label:"positive", score:0.9},{label:"neutral", score:0.6},{label:"negative", score:0.7}])` returns `{positive:33, neutral:33, negative:34, sampleSize:3}` (or any non-negative integer trio summing to exactly 100 — rounding is implementation-defined but the three fields MUST sum to 100).
    - Test 4: `aggregateSentiment([])` returns `{positive:0, neutral:100, negative:0, sampleSize:0}` (or any default where the three percentages sum to 100 and `sampleSize === 0`).
    - Test 5: When `TOGETHER_API_KEY` is unset, `fetchAndCacheSentiment([{text:"x"}])` returns the same "no-data default" without making a network call (env guard).
    - Test 6: When the mocked fetch returns 401, the function calls `console.error` with a string matching `/401/` AND (if `NEXT_PUBLIC_SENTRY_DSN` set) `Sentry.captureException`.
  </behavior>
  <action>
    Create `lib/api/sentiment.ts`. Imports: `import * as Sentry from "@sentry/nextjs"`, `import { getRedis } from "@/lib/cache/redis"`, `import { cacheSet } from "@/lib/cache/helpers"`, `import { CACHE_KEYS } from "@/lib/constants"`, `import type { Sentiment } from "@/lib/types"`.
    
    Define and export pure function `preprocessText(text: string): string` (F2 contract — CON-cardiffnlp-preprocessing): replace `@<handle>` (regex `/@[A-Za-z0-9_]+/g`) with the literal `@user`; replace any `https?:\/\/\S+` (regex `/https?:\/\/\S+/g`) with the literal `http`. Apply BOTH replacements in order. Return the transformed string. Must be deterministic — no randomness.
    
    Define and export pure function `aggregateSentiment(predictions: Array<{ label: "positive" | "neutral" | "negative"; score: number }>): Sentiment`. Count occurrences of each label (predictions are pre-argmaxed by the vendor — we just tally). Compute integer percentages summing EXACTLY to 100. Use largest-remainder rounding so rounding errors don't break the sum-to-100 contract (acceptance criterion 1 in ROADMAP). When `predictions` is empty, return `{positive: 0, neutral: 100, negative: 0, sampleSize: 0}` as a safe default that still sums to 100.
    
    Define private async `checkAndConsumeBudget(charsNeeded: number): Promise<boolean>` — daily char-budget circuit breaker (D1/F1/CON-hf-quota-budget). The budget value is `Number(process.env.SENTIMENT_DAILY_CHAR_BUDGET ?? "200000")` (default 200000). Use Redis key `sentiment:budget:YYYY-MM-DD` (today's date in UTC, ISO format `YYYY-MM-DD`). Algorithm: get current value via `redis.get<number>(key) ?? 0`. If `current + charsNeeded > budget`, return false (do NOT call the vendor). Else `redis.incrby(key, charsNeeded)` AND `redis.expire(key, 86400 * 2)` (keep ~48h so day-boundary edge cases don't crash). Return true.
    
    Define and export `async function fetchAndCacheSentiment(items: Array<{ text: string }>): Promise<Sentiment>`:
    (a) Env guard: `const apiKey = process.env.TOGETHER_API_KEY; if (!apiKey) { console.warn("[sentiment] TOGETHER_API_KEY not set, skipping"); return aggregateSentiment([]); }` (NEVER reference `HUGGINGFACE_API_TOKEN` — F1 forbids).
    (b) Preprocess each item via `preprocessText`. Drop empty results.
    (c) Truncate to batch size 100 (Together AI limit per CONTEXT line 110 — Claude's discretion notes ≤100).
    (d) Char-budget check: `const totalChars = batched.reduce((s, t) => s + t.length, 0); if (!(await checkAndConsumeBudget(totalChars))) { console.warn("[sentiment] daily char budget tripped"); return aggregateSentiment([]); }`.
    (e) POST to Together AI inference endpoint with `Authorization: Bearer ${apiKey}` (mirror `lib/api/twitter.ts:37-50` shape). Model: `cardiffnlp/twitter-roberta-base-sentiment-latest`. Send the batch as input. Implementation detail (Claude's discretion per CONTEXT line 110): pick either Together's text-classification endpoint or chat-completions with structured output. Document the exact endpoint URL inline.
    (f) Handle non-OK responses. SPECIAL CASE for `res.status === 401` (CON-key-rotation-observability): `console.error("[sentiment] 401 from Together AI — TOGETHER_API_KEY may be expired or rotated");` AND `if (process.env.NEXT_PUBLIC_SENTRY_DSN) { Sentry.captureException(new Error("Together AI 401 — key rotation suspected")); }`. Do NOT silently swallow. Return `aggregateSentiment([])` (safe default).
    (g) Parse response into `Array<{ label, score }>`. Call `const sentiment = aggregateSentiment(predictions)`.
    (h) `await cacheSet(CACHE_KEYS.sentiment, sentiment)`.
    (i) Return `sentiment`.
    
    Timeout: wrap the fetch in `AbortController` with a 5s timeout (CON-sentiment-5s-budget). On timeout, log + capture in Sentry + return `aggregateSentiment([])`.
    
    Do NOT export `checkAndConsumeBudget` (private). Do NOT export the model name as a constant unless it's referenced from a test. Do NOT introduce a stale-fallback path (deferred to v2).
  </action>
  <verify>
    <automated>cd /Users/akshaydhaliwal/claude-code/ai-dashboard && npx tsc --noEmit && grep -cE '^export (async )?function (preprocessText|aggregateSentiment|fetchAndCacheSentiment)' lib/api/sentiment.ts | awk '$1 == 3 {exit 0} {exit 1}' && ! grep -i 'HUGGINGFACE' lib/api/sentiment.ts && grep -q 'TOGETHER_API_KEY' lib/api/sentiment.ts && grep -q 'SENTIMENT_DAILY_CHAR_BUDGET' lib/api/sentiment.ts && grep -q '401' lib/api/sentiment.ts && grep -q 'Sentry.captureException' lib/api/sentiment.ts</automated>
  </verify>
  <done>
    File compiles. Three named exports present. `TOGETHER_API_KEY` and `SENTIMENT_DAILY_CHAR_BUDGET` referenced; `HUGGINGFACE` does not appear; explicit 401 + Sentry handling present.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write lib/api/sentiment.test.ts with preprocessText + aggregation coverage</name>
  <read_first>
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/api/sentiment.ts (system under test, from task 1)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-CONTEXT.md (D6 + F2 — required coverage for `preprocessText` AND aggregation summing to 100)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/intel/constraints.md (CON-cardiffnlp-preprocessing — exact contract)
  </read_first>
  <action>
    Create `lib/api/sentiment.test.ts`. Import `describe`, `it`, `expect` from `"vitest"`. Import `{ preprocessText, aggregateSentiment }` from `"@/lib/api/sentiment"`. Test cases (the six behaviors above — focus on the pure functions; do NOT exercise `fetchAndCacheSentiment` against a real network):
    (1) `expect(preprocessText("Hello @karpathy check https://x.com/foo")).toBe("Hello @user check http")`.
    (2) `expect(preprocessText("plain text")).toBe("plain text")`.
    (3) Edge case: multiple handles — `expect(preprocessText("@a hi @b"))` should equal `"@user hi @user"`.
    (4) Edge case: http (no s) — `expect(preprocessText("see http://x.com"))` should equal `"see http"`.
    (5) `aggregateSentiment` with one of each label returns three integers summing to 100 — `const r = aggregateSentiment([{label:"positive",score:1},{label:"neutral",score:1},{label:"negative",score:1}]); expect(r.positive + r.neutral + r.negative).toBe(100); expect(r.sampleSize).toBe(3);`.
    (6) `aggregateSentiment([])` returns three percentages summing to 100 and `sampleSize === 0`.
    (7) `aggregateSentiment` with skewed counts (10 positives, 0 neutral, 0 negative) returns `{positive: 100, neutral: 0, negative: 0, sampleSize: 10}`.
    Do NOT mock fetch — the test file covers pure-function core only (per D6 + CONTEXT line 112 Claude's Discretion). Do NOT touch the network.
  </action>
  <verify>
    <automated>cd /Users/akshaydhaliwal/claude-code/ai-dashboard && npx vitest run lib/api/sentiment.test.ts --reporter=basic 2>&1 | tee /tmp/vitest-sentiment.log && grep -qE '(Test Files.*1 passed|Tests.*[6-9] passed)' /tmp/vitest-sentiment.log</automated>
  </verify>
  <done>
    `npx vitest run lib/api/sentiment.test.ts` exits 0. At least 6 tests pass covering both `preprocessText` (F2 contract) and aggregation (sum-to-100 invariant).
  </done>
</task>

<task type="auto">
  <name>Task 3: Create app/api/sentiment/route.ts (read-only cache endpoint, 503 on cold-miss)</name>
  <read_first>
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/app/api/trending/route.ts (the EXACT pattern to mirror — lines 1-23)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/constants.ts (plan 03 — `CACHE_KEYS.sentiment`, `CACHE_MAX_AGE.default`)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/types.ts (plan 03 — `Sentiment` type)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/REQUIREMENTS.md (REQ-sentiment-engine acceptance: cold-miss returns 503, no stale fallback)
  </read_first>
  <action>
    Create `app/api/sentiment/route.ts`. Mirror `app/api/trending/route.ts` exactly. Imports: `import { NextResponse } from "next/server"`, `import { cacheGet } from "@/lib/cache/helpers"`, `import { CACHE_KEYS, CACHE_MAX_AGE } from "@/lib/constants"`, `import type { Sentiment } from "@/lib/types"`. Export `const dynamic = "force-dynamic"`. Export `async function GET()`: `const cached = await cacheGet<Sentiment>(CACHE_KEYS.sentiment, CACHE_MAX_AGE.default)`. If `!cached`, return `NextResponse.json({ data: null, cachedAt: null, stale: false, error: "No data available" }, { status: 503 })` (cold-miss 503, no stale fallback). Else return `NextResponse.json({ data: cached.data, cachedAt: cached.fetchedAt, stale: cached.stale })`. Note: the `data` field on cold-miss is `null` (not `[]`) because `Sentiment` is a singular object, not an array — this is the only structural deviation from `app/api/trending/route.ts`.
  </action>
  <verify>
    <automated>cd /Users/akshaydhaliwal/claude-code/ai-dashboard && npx tsc --noEmit && grep -q 'force-dynamic' app/api/sentiment/route.ts && grep -q 'CACHE_KEYS.sentiment' app/api/sentiment/route.ts && grep -q 'status: 503' app/api/sentiment/route.ts && grep -q 'cacheGet<Sentiment>' app/api/sentiment/route.ts</automated>
  </verify>
  <done>
    Route compiles. `GET /api/sentiment` exists. Cold-miss returns 503. `force-dynamic` set.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean
- `npx vitest run lib/api/sentiment.test.ts` exits 0
- `grep -r HUGGINGFACE lib/api/sentiment.ts app/api/sentiment/route.ts` returns nothing
- `grep -c TOGETHER_API_KEY lib/api/sentiment.ts` >= 1
- `grep -c '401' lib/api/sentiment.ts` >= 1
- `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/api/sentiment` returns 503 on cold start (manual / integration check — not part of automated gate)
</verification>

<success_criteria>
- REQ-sentiment-engine met across the three artifacts: route + engine + test
- F1 / F2 / D7 / CON-key-rotation-observability all honored
- 5s budget left to plan 09's cron wiring (timeout enforced at the engine level)
- Pure-function core (`preprocessText` + `aggregateSentiment`) tested
</success_criteria>

<output>
After completion, create `.planning/phases/01-scrum-38-implementation/01-07-SUMMARY.md` per @$HOME/.claude/get-shit-done/templates/summary.md.
</output>
