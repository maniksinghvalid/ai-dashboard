---
phase: 01-scrum-38-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
autonomous: true
requirements:
  - REQ-vitest-bootstrap
must_haves:
  truths:
    - "`npm test` exits 0 (zero test files yet is acceptable on bootstrap — Vitest with no matched tests still exits 0 with a 'no tests' notice; downstream plans add the 5 required tests)"
    - "Vitest resolves `@/*` imports the same way `tsconfig.json` does"
    - "`vitest`, `@vitest/coverage-v8`, `tsx` appear in `package.json` devDependencies"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest config with `environment: 'node'` and `@/*` alias to project root"
      contains: "environment: \"node\""
    - path: "package.json"
      provides: "scripts.test = 'vitest run', scripts.test:watch = 'vitest', scripts.test:coverage = 'vitest run --coverage'"
      contains: "\"test\": \"vitest run\""
  key_links:
    - from: "vitest.config.ts"
      to: "tsconfig.json"
      via: "path alias `@/*` -> project root"
      pattern: "resolve.alias.*@.*path.resolve"
---

<objective>
Bootstrap the project's first test framework. Install Vitest + coverage + tsx, add three npm scripts, write `vitest.config.ts` with `environment: "node"` and a `@/*` alias matching `tsconfig.json`. This plan ships ONLY the framework — the five required test files are written by their respective feature plans (02, 04, 05, 06, 07).

Purpose: Unblocks every downstream plan that ships a test file (D6 locked decision). Without this, `npm test` does not exist as a command.
Output: `vitest.config.ts`, updated `package.json` scripts + devDependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/PROJECT.md
@/Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/ROADMAP.md
@/Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/STATE.md
@/Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-CONTEXT.md
@/Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-PATTERNS.md
@/Users/akshaydhaliwal/claude-code/ai-dashboard/CLAUDE.md
@/Users/akshaydhaliwal/claude-code/ai-dashboard/package.json
@/Users/akshaydhaliwal/claude-code/ai-dashboard/tsconfig.json

<interfaces>
<!-- From tsconfig.json:20-22 — replicate in vitest.config.ts -->
"paths": { "@/*": ["./*"] }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest dependencies and add npm scripts</name>
  <read_first>
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/package.json (current scripts + devDependencies layout)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-CONTEXT.md (D6 locked decision: vitest + @vitest/coverage-v8 + tsx; scripts: test, test:watch, test:coverage)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-PATTERNS.md ("package.json (extended — Vitest deps + scripts)" section)
  </read_first>
  <action>
    Install devDependencies via `npm install --save-dev vitest @vitest/coverage-v8 tsx` (per D6). Add three scripts to `package.json` scripts block adjacent to the existing `lint` entry, exact strings: `"test": "vitest run"`, `"test:watch": "vitest"`, `"test:coverage": "vitest run --coverage"`. Do NOT modify any existing script. Do NOT add `jest` or any other test framework — Vitest is the locked choice (D6). Verify `npm install` succeeds and `package-lock.json` is updated with the three new dev deps.
  </action>
  <verify>
    <automated>cd /Users/akshaydhaliwal/claude-code/ai-dashboard && grep -E '"(test|test:watch|test:coverage|vitest|@vitest/coverage-v8|tsx)"' package.json | grep -v '^#' | wc -l | awk '$1 >= 6 {exit 0} {exit 1}'</automated>
  </verify>
  <done>
    `package.json` contains all three scripts AND all three devDependencies. `npm test` is now a runnable command (it will print "No test files found" until plan 02 ships the first test, which is fine — exit 0 either way).
  </done>
</task>

<task type="auto">
  <name>Task 2: Write vitest.config.ts with node environment and @/* alias</name>
  <read_first>
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/tsconfig.json (lines 20-22 — `@/*` -> `./*` is the alias source of truth)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-PATTERNS.md ("vitest.config.ts" section — suggested skeleton)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-CONTEXT.md (D6: `environment: "node"`; `@/*` alias to project root)
  </read_first>
  <action>
    Create `vitest.config.ts` at project root. Use `defineConfig` from `vitest/config`. Set `test.environment` to `"node"` (per D6 — pure-function tests only, never DOM). Set `test.include` to `["**/*.test.ts"]`. Set `resolve.alias["@"]` to `path.resolve(__dirname, ".")` so `@/lib/...` imports in tests resolve to the project root, matching `tsconfig.json`'s `paths` mapping. Import `path` from `"node:path"`. Do NOT add jsdom, happy-dom, or browser environments. Do NOT add `setupFiles` — boundary mocking happens inline in each test file (per D6 / CONTEXT.md "Vitest mocking" discretion line).
  </action>
  <verify>
    <automated>cd /Users/akshaydhaliwal/claude-code/ai-dashboard && test -f vitest.config.ts && grep -q 'environment: "node"' vitest.config.ts && grep -q 'path.resolve' vitest.config.ts && npx vitest run --reporter=basic 2>&1 | grep -qE '(No test files found|0 passed|Test Files)'</automated>
  </verify>
  <done>
    `vitest.config.ts` exists; `npm test` exits 0; running it from any test file in plans 02/04/05/06/07 will resolve `@/lib/...` imports correctly.
  </done>
</task>

</tasks>

<verification>
- `npm test` runs without crashing (exit 0)
- `cat package.json | grep -E '"test"|"vitest"' ` shows all three scripts and all three devDeps
- `cat vitest.config.ts` shows `environment: "node"` and the `@` alias
- `node -e 'require("./vitest.config.ts")'` is NOT a valid check (TS file) — use `npx vitest run --reporter=basic` instead
</verification>

<success_criteria>
- Vitest installed and runnable
- Config supports `@/*` alias for downstream tests
- No legacy test framework (Jest, Mocha) introduced
- Acceptance criterion 7 from ROADMAP partially met (test framework present; the 5 required test files are added by downstream plans)
</success_criteria>

<output>
After completion, create `.planning/phases/01-scrum-38-implementation/01-01-SUMMARY.md` per @$HOME/.claude/get-shit-done/templates/summary.md.
</output>
