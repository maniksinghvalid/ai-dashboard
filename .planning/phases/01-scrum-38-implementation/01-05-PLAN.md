---
phase: 01-scrum-38-implementation
plan: 05
type: execute
wave: 2
depends_on: ["01-02", "01-03", "01-04"]
files_modified:
  - lib/api/trending.ts
  - lib/api/trending.test.ts
autonomous: true
requirements:
  - REQ-trending-velocity
must_haves:
  truths:
    - "`/api/trending` returns topics ranked by `(count_now − count_1h_ago)` per-hour velocity (window is 3600s = 1h) — old `count * sources.length * log2(velocity+1)` heuristic is GONE"
    - "Topics come from `AI_TOPICS` (plan 02), not the deleted inline `AI_TERMS`"
    - "Word-boundary topics like 'agents' do NOT match 'agentsystem' (per plan 02's `matchTopic`)"
    - "For each `(topic, platform)`, the code calls `zaddTimepoint` and `capToSlots(..., 96)` (per D2 storage shape)"
    - "Ranked output written to BOTH `CACHE_KEYS.trending` (backward-compat for `/api/trending`) AND `CACHE_KEYS.trendingRanked` (10-min TTL via tenMin)"
    - "`getAlertsRows(now)` returns canonical `(topicId, topic, platform, velocity)` rows where `topicId` is the literal `AI_TOPICS[k].id` (never a derived slug)"
  artifacts:
    - path: "lib/api/trending.ts"
      provides: "Velocity-ranked trending using ZSET storage + canonical alerts-row helper"
      exports: ["calculateTrending", "fetchAndCacheTrending", "pickHeroCandidate", "getAlertsRows"]
      contains: "zaddTimepoint"
    - path: "lib/api/trending.test.ts"
      provides: "Velocity-calc tests + hero-threshold helper tests + canonical-row test"
      contains: "describe"
  key_links:
    - from: "lib/api/trending.ts"
      to: "lib/cache/timeseries.ts (plan 04)"
      via: "zaddTimepoint / zrangeWindow / capToSlots"
      pattern: "import.*zaddTimepoint.*from.*@/lib/cache/timeseries"
    - from: "lib/api/trending.ts"
      to: "lib/topics.ts (plan 02)"
      via: "AI_TOPICS + matchTopic"
      pattern: "import.*AI_TOPICS.*from.*@/lib/topics"
---

<objective>
Refactor `lib/api/trending.ts` to (1) source topics from `AI_TOPICS` (plan 02), (2) write `(topic, platform)` counts to ZSETs via `zaddTimepoint` + `capToSlots(..., 96)` (plan 04), (3) compute per-hour velocity as `(count_now − count_1h_ago)` over a 1h window via `zrangeWindow`, (4) sort by velocity (not the log2 heuristic), (5) cache to both `CACHE_KEYS.trending` (preserve `/api/trending` compatibility) and `CACHE_KEYS.trendingRanked`. Also expose two pure helpers: `pickHeroCandidate` (consumed by plan 08) and `getAlertsRows` (consumed by plan 09 — emits canonical per-(topic, platform) rows so spike detection compares apples-to-apples against the per-platform 24h baseline in plan 06). The test file pins all three contracts (REQ-trending-velocity acceptance + hero-threshold per Roadmap success criterion 7 + canonical-topicId invariant).

Purpose: Replaces the heuristic scorer (REQ-trending-velocity). Word-boundary matching via plan 02 means "agents" no longer matches "agentsystem". Canonical alerts rows prevent the previous-iteration bug where plan 09 reconstructed `topicId` from a display label (incoherent for topics whose label differs from their id).
Output: Refactored `lib/api/trending.ts`, new `lib/api/trending.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-CONTEXT.md
@/Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-PATTERNS.md
@/Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/intel/constraints.md
@/Users/akshaydhaliwal/claude-code/ai-dashboard/CLAUDE.md
@/Users/akshaydhaliwal/claude-code/ai-dashboard/lib/api/trending.ts
@/Users/akshaydhaliwal/claude-code/ai-dashboard/lib/cache/timeseries.ts
@/Users/akshaydhaliwal/claude-code/ai-dashboard/lib/topics.ts
@/Users/akshaydhaliwal/claude-code/ai-dashboard/lib/constants.ts
@/Users/akshaydhaliwal/claude-code/ai-dashboard/app/api/trending/route.ts

<interfaces>
From lib/topics.ts (plan 02):
  export type Topic = { id: string; label: string; aliases: string[]; wordBoundary?: boolean };
  export const AI_TOPICS: Topic[];
  export function matchTopic(text: string, topic: Topic): boolean;

From lib/cache/timeseries.ts (plan 04):
  export function zaddTimepoint(key: string, ts: number, count: number): Promise<void>;
  export function zrangeWindow(key: string, fromTs: number, toTs: number): Promise<Array<{ ts: number; count: number }>>;
  export function capToSlots(key: string, maxEntries: number): Promise<void>;

From lib/constants.ts (plan 03):
  CACHE_KEYS.trending = "trending:topics" (existing — keep writing here for /api/trending compat);
  CACHE_KEYS.trendingRanked = "trending:ranked" (new — 10-min TTL via CACHE_MAX_AGE.tenMin);
  CACHE_MAX_AGE.tenMin = 10 * 60 * 1000.

Current fetchAndCacheTrending signature (lib/api/trending.ts:111-119) — KEEP for cron compat:
  export async function fetchAndCacheTrending(videos: Video[], posts: RedditPost[], tweets: Tweet[]): Promise<TrendingTopic[]>

Canonical alerts-row shape (locked here for plan 06 + plan 09 consumers):
  Array<{ topicId: string; topic: string; platform: "youtube" | "reddit" | "twitter"; velocity: number }>
  WHERE topicId === AI_TOPICS[k].id (NEVER a derived slug from the display label)
  AND velocity is the PER-PLATFORM value (NOT the cross-platform aggregate)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor lib/api/trending.ts to consume AI_TOPICS + timeseries + velocity math, expose pickHeroCandidate + getAlertsRows</name>
  <read_first>
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/api/trending.ts (full file — lines 5-14 = `AI_TERMS` to delete; 56-109 = heuristic scorer to replace; 111-119 = `fetchAndCacheTrending` signature to PRESERVE)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/topics.ts (plan 02 — `AI_TOPICS`, `matchTopic`)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/cache/timeseries.ts (plan 04 — the three exports)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/constants.ts (plan 03 — `CACHE_KEYS.trending`, `CACHE_KEYS.trendingRanked`, `CACHE_MAX_AGE.tenMin`)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/cache/helpers.ts (`cacheSet` — keep using for the ranked write)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/intel/constraints.md (CON-trending-storage-shape: one ZSET per `(topic, platform)`, 96-slot cap, no snapshot rotation; CON-timeseries-api: must consume the wrapper)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/app/api/trending/route.ts (downstream consumer — reads `CACHE_KEYS.trending`; output shape `TrendingTopic[]` must not break)
  </read_first>
  <behavior>
    - Test 1: After two `recordCountsToZsets({ topic: "claude", platform: "youtube" }, [{ ts: 1000, count: 3 }])` ticks, `computeVelocityForTopicPlatform(...)` returns `latestCount - hourAgoCount` (per-hour velocity over the 1h window) — verified by mocked `zrangeWindow` returns.
    - Test 2: `calculateTrending` no longer uses `Math.log2` (regex source check) and no longer references `AI_TERMS` (regex source check on the file).
    - Test 3: For a topic with `wordBoundary: true`, an item with text "agentsystem" is NOT counted (proves `matchTopic` is used, not inline substring).
    - Test 4: Output `TrendingTopic[]` is sorted by `velocity` descending (not `score` descending). The legacy `score` field MAY still be populated for backward-compat with the API response shape.
    - Test 5: `pickHeroCandidate(perPlatformRanked)` (the helper plan 08 will consume) returns the topic with the highest aggregate velocity that appears in the top 3 of ALL three platforms; returns `null` when no topic qualifies.
    - Test 6 (canonical-row invariant — fixes prior-iteration BLOCKER 1+2): `getAlertsRows(now)` returns rows where EVERY row's `topicId` is exactly one of `AI_TOPICS[k].id` (compared by literal string equality). No row has `topicId` derived from `topic.toLowerCase().replace(/\s+/g, "-")` or any other label-derived slug.
    - Test 7: `getAlertsRows(now)` emits exactly one row per non-zero `(topic, platform)` pair across {youtube, reddit, twitter} (news is excluded — not tracked per-platform here). Zero-velocity rows are filtered out.
  </behavior>
  <action>
    Refactor `lib/api/trending.ts` in place. STEP A — delete the inline `AI_TERMS` array (lines 5-14). STEP B — add imports: `{ AI_TOPICS, matchTopic, type Topic } from "@/lib/topics"`, `{ zaddTimepoint, zrangeWindow, capToSlots } from "@/lib/cache/timeseries"`, extend the `CACHE_KEYS` and `CACHE_MAX_AGE` named-imports from `@/lib/constants`. STEP C — preserve the existing `TextItem` extraction shape (lines 16-54) and the public function `fetchAndCacheTrending(videos, posts, tweets)` signature (line 111) — the cron in plan 09 already calls this. STEP D — replace the scorer (lines 56-109) with:
    (i) `countMentionsByTopicPlatform(items: TextItem[]): Map<string, number>` — pure function returning a `Map` keyed by `\`${topic.id}:${platform}\`` (where platform is `"youtube" | "reddit" | "twitter"`) of mention counts for the CURRENT cron tick. Iterates `AI_TOPICS` and uses `matchTopic(item.text, topic)` (NEVER `.toLowerCase().includes(...)` directly). The `(topic, platform)` ZSET key string MUST be `\`trending:ts:${topic.id}:${platform}\`` — pick this exact string format and use it consistently. CRITICAL: the map key MUST be `${topic.id}:${platform}` so downstream consumers (getAlertsRows) parse out the canonical `topic.id`, NOT a derived slug.
    (ii) `recordCountsToZsets(counts: Map<string,number>, now: number): Promise<void>` — for each `(topic, platform)` entry, calls `await zaddTimepoint(key, now, count)` then `await capToSlots(key, 96)` (D2 cap).
    (iii) `computeVelocityForTopicPlatform(topicId: string, platform: string, now: number): Promise<number>` — async; reads two windows via `zrangeWindow`: `windowNow = zrangeWindow(key, now - 3600, now)` (last hour) and `windowPrev = zrangeWindow(key, now - 7200, now - 3600)` (the hour before). Sums `count` over each window. Returns `sumNow - sumPrev` with a trailing comment `// per-hour velocity (window is 3600s = 1h)` — do NOT divide by 1; the original "/ 1h" in the CONTEXT formula is dimensional (per-hour), not a literal divisor. Negative velocities are allowed (decaying topics).
    (iv) `pickHeroCandidate(ranked: Record<"youtube"|"reddit"|"twitter", TrendingTopic[]>): TrendingTopic | null` — pure helper for plan 08. Filters to topics appearing in `ranked.youtube.slice(0,3)` AND `ranked.reddit.slice(0,3)` AND `ranked.twitter.slice(0,3)` by `topic` field. From qualifying topics, picks the one with highest `aggregateVelocity` (sum of velocities across the three platforms). Returns `null` if no qualifying topic.
    (v) `getAlertsRows(now: number): Promise<Array<{ topicId: string; topic: string; platform: "youtube" | "reddit" | "twitter"; velocity: number }>>` — async; the canonical source-of-truth row shape for plan 06's `writeSpikeAlertsFromTrending`. Iterates `AI_TOPICS × {youtube, reddit, twitter}` (NEWS IS EXCLUDED — not tracked per-platform here). For each `(topic, platform)` pair, awaits `computeVelocityForTopicPlatform(topic.id, platform, now)`. Emits a row `{ topicId: topic.id, topic: topic.label, platform, velocity }` where `topicId` is the LITERAL `topic.id` from `AI_TOPICS` (NEVER `topic.label.toLowerCase().replace(...)` — that would break for topics whose label differs from id). The `velocity` field is the PER-PLATFORM value, NOT the cross-platform aggregate (so plan 06's per-platform 24h baseline compares apples-to-apples). Filter out rows where `velocity === 0` (no signal, no spike check needed). Use `Promise.all` over the topic×platform grid for parallelism. Export this function.
    (vi) Rewrite `calculateTrending(videos, posts, tweets)`: extract `items` (existing helper), call `countMentionsByTopicPlatform(items)`, return the per-platform aggregated array as `TrendingTopic[]` (one row per topic per platform with `topic`, `mentionCount`, `velocity` (computed against ZSETs — for the synchronous call path, accept that velocity can be 0 on cold start; the async write step provides the data for the NEXT cycle), `sources` (the set of platforms with non-zero counts), `score: velocity`). Sort by `velocity` descending. SLICE to top 20.
    (vii) Rewrite `fetchAndCacheTrending(videos, posts, tweets)`: now async, accepts the same args. Steps: (a) `const items = extractTextItems(videos, posts, tweets)`; (b) `const counts = countMentionsByTopicPlatform(items)`; (c) `const now = Math.floor(Date.now() / 1000)`; (d) `await recordCountsToZsets(counts, now)`; (e) build per-platform ranked maps by reading `zrangeWindow` for each `(topic, platform)` (parallel via `Promise.all`); (f) compose final `TrendingTopic[]` sorted by aggregate velocity; (g) `await cacheSet(CACHE_KEYS.trending, topics)` (backward-compat for `/api/trending`); (h) `await cacheSet(CACHE_KEYS.trendingRanked, topics)` (new key — 10-min TTL is enforced by the route's `CACHE_MAX_AGE.tenMin` read, not by the write).
    Export `pickHeroCandidate` (plan 08 imports it), `getAlertsRows` (plan 09 imports it), and the helpers (i-iii) so the test file in task 2 can exercise them directly. Do NOT introduce any new env vars. Do NOT call `getRedis()` directly — go through `lib/cache/timeseries.ts`. Do NOT use `Math.log2` anywhere. Do NOT derive `topicId` from any display label anywhere in this module.
  </action>
  <verify>
    <automated>cd /Users/akshaydhaliwal/claude-code/ai-dashboard && npx tsc --noEmit && ! grep -E 'Math\.log2|^const AI_TERMS' lib/api/trending.ts && grep -q 'AI_TOPICS' lib/api/trending.ts && grep -q 'zaddTimepoint' lib/api/trending.ts && grep -q 'capToSlots.*96' lib/api/trending.ts && grep -q 'pickHeroCandidate' lib/api/trending.ts && grep -qE '^export (async )?function getAlertsRows' lib/api/trending.ts && ! grep -E 'topic\.label\.toLowerCase\(\)\.replace|topic\.topic\.toLowerCase\(\)\.replace' lib/api/trending.ts</automated>
  </verify>
  <done>
    File compiles. `Math.log2` and `AI_TERMS` gone. `AI_TOPICS`, `zaddTimepoint`, `capToSlots(..., 96)`, `pickHeroCandidate`, and `getAlertsRows` all present. `fetchAndCacheTrending(videos, posts, tweets)` signature unchanged so the cron keeps building. Both `CACHE_KEYS.trending` and `CACHE_KEYS.trendingRanked` are written. Velocity formula uses `sumNow - sumPrev` (NOT `/ 1`). No label-derived slugs anywhere.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write lib/api/trending.test.ts covering velocity calc + hero threshold + canonical-row invariant</name>
  <read_first>
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/api/trending.ts (system under test, post-refactor)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/cache/timeseries.ts (mock at this boundary)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/topics.ts (AI_TOPICS — the canonical id source for test 6)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-CONTEXT.md (D6: required test coverage for trending = velocity calc + hero promotion threshold)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-PATTERNS.md ("Test files" section)
  </read_first>
  <acceptance_criteria>
    - All 7 test cases below pass via `npx vitest run lib/api/trending.test.ts`.
    - No real Redis access; `@/lib/cache/timeseries` and `@/lib/cache/helpers` are mocked.
    - Test 6 (canonical-row invariant) asserts literal string equality with `AI_TOPICS[k].id`, not regex / contains.
  </acceptance_criteria>
  <action>
    Create `lib/api/trending.test.ts`. Import `describe`, `it`, `expect`, `vi`, `beforeEach` from `"vitest"`. Mock the timeseries module via `vi.mock("@/lib/cache/timeseries", () => ({ zaddTimepoint: vi.fn(), zrangeWindow: vi.fn(), capToSlots: vi.fn() }))`. Mock `@/lib/cache/helpers` (`cacheSet: vi.fn()`) so no real Redis write occurs. Import the SUT: `{ countMentionsByTopicPlatform, recordCountsToZsets, computeVelocityForTopicPlatform, pickHeroCandidate, getAlertsRows } from "@/lib/api/trending"` (note: these are exported by task 1 specifically for testing). Also import `{ AI_TOPICS } from "@/lib/topics"` for test 6's literal id assertion. Test cases:
    (a) `countMentionsByTopicPlatform` with three items mentioning "Claude" on youtube/reddit/twitter returns a Map with three entries each = 1.
    (b) `countMentionsByTopicPlatform` with text "agentsystem release" does NOT count toward the `agents` topic (proves `wordBoundary` flows through).
    (c) `computeVelocityForTopicPlatform`: with `zrangeWindow` mocked to return `[{ts: 100, count: 5}, {ts: 200, count: 7}]` for the now-window and `[{ts: -100, count: 2}]` for the prev-window, expect velocity = `12 - 2 = 10` (per-hour velocity, no literal divisor).
    (d) `recordCountsToZsets({Map of one entry}, now)` invokes `zaddTimepoint` once with `(expectedKey, now, count)` AND `capToSlots(expectedKey, 96)` once.
    (e) `pickHeroCandidate({youtube: [{topic:"claude",velocity:5,...},...], reddit: [{topic:"claude",velocity:3,...},...], twitter: [{topic:"claude",velocity:2,...},...]})` where "claude" is in the top-3 of all three returns the "claude" row with the highest aggregate velocity (10).
    (f) `pickHeroCandidate({youtube: [{topic:"a",...}], reddit: [{topic:"b",...}], twitter: [{topic:"c",...}]})` with no overlap returns `null`.
    (g) CANONICAL-ROW INVARIANT (fixes prior-iteration BLOCKER 1+2): mock `zrangeWindow` to return one entry with `count: 1` for every call (so every `(topic, platform)` pair yields a non-zero velocity). Call `const rows = await getAlertsRows(Math.floor(Date.now() / 1000))`. For each row, assert `AI_TOPICS.some(t => t.id === row.topicId)` is true — i.e., the returned `topicId` MUST be a literal `AI_TOPICS[k].id`, never a derived slug. Additionally assert at least one row has a `topicId` that differs from its `topic` label (proves the test would catch the prior bug where `topicId = topic.toLowerCase().replace(...)` was used; pick a topic in `AI_TOPICS` whose `id` ≠ lowercased label — e.g., "arc-agi" vs label "ARC-AGI").
    Do NOT touch the network. Do NOT import the actual Upstash redis client.
  </action>
  <verify>
    <automated>cd /Users/akshaydhaliwal/claude-code/ai-dashboard && npx vitest run lib/api/trending.test.ts --reporter=basic 2>&1 | tee /tmp/vitest-trending.log && grep -qE '(Test Files.*1 passed|Tests.*[7-9] passed)' /tmp/vitest-trending.log</automated>
  </verify>
  <done>
    `npx vitest run lib/api/trending.test.ts` exits 0. At least 7 test cases pass covering velocity + hero threshold + word-boundary flow-through + canonical-row invariant. No real Redis access.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean
- `npx vitest run lib/api/trending.test.ts` exits 0
- `grep -v '^#' lib/api/trending.ts | grep -c 'Math.log2'` == 0
- `grep -v '^#' lib/api/trending.ts | grep -c 'AI_TERMS'` == 0
- `grep -c 'zaddTimepoint' lib/api/trending.ts` >= 1
- `grep -c 'pickHeroCandidate' lib/api/trending.ts` >= 1
- `grep -c 'getAlertsRows' lib/api/trending.ts` >= 1
- `/api/trending` route (`app/api/trending/route.ts`) still compiles and reads `CACHE_KEYS.trending`
</verification>

<success_criteria>
- REQ-trending-velocity acceptance met: velocity computed via `zrangeWindow(now-3600, now)` − `zrangeWindow(now-7200, now-3600)` against ZSETs (D2), formula is `sumNow - sumPrev` (per-hour over the 1h window)
- Topics ranked by velocity (not log2 heuristic)
- Topics sourced from `AI_TOPICS` (D5)
- Word-boundary matching for "agents" works correctly
- `pickHeroCandidate` exported for plan 08
- `getAlertsRows` exported for plan 09 — emits canonical `(topicId === AI_TOPICS[k].id, topic, platform, velocity)` rows with per-platform velocity (fixes prior-iteration coherence bug)
</success_criteria>

<output>
After completion, create `.planning/phases/01-scrum-38-implementation/01-05-SUMMARY.md` per @$HOME/.claude/get-shit-done/templates/summary.md.
</output>
