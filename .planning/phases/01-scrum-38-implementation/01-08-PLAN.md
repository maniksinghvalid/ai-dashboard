---
phase: 01-scrum-38-implementation
plan: 08
type: execute
wave: 2
depends_on: ["01-03", "01-05"]
files_modified:
  - lib/api/hero.ts
  - lib/api/hero.test.ts
  - app/api/hero/route.ts
autonomous: true
requirements:
  - REQ-hero-auto-promotion
must_haves:
  truths:
    - "A topic is hero-eligible when it appears in the top-3 on YouTube AND Reddit AND X simultaneously, ranked by aggregate velocity (D3 relaxation from #1 on all 3)"
    - "app/api/hero/route.ts contains an inline comment documenting the top-3-on-all-3 relaxation (CON-hero-relaxation-disclosure locked)"
    - "Cold-miss / no qualifying topic returns 503 (no stale fallback)"
    - "Cached at CACHE_KEYS.hero (10-min TTL via CACHE_MAX_AGE.tenMin)"
    - "Promoter consumes pickHeroCandidate from lib/api/trending.ts (plan 05) — does NOT reimplement the top-3 logic"
    - "Hero fusion uses top video + top post + top news (per REQ-hero-auto-promotion: 'fuse top video + top post + tweet count') — matching Reddit posts contribute to headline and link fallback chain"
  artifacts:
    - path: "lib/api/hero.ts"
      provides: "promoteHero() — runs in cron Tier 3, fuses top video + post + tweet count into HeroStory"
      exports: ["promoteHero"]
    - path: "lib/api/hero.test.ts"
      provides: "Unit tests for hero fusion fallback chain (incl. Reddit-only fallback case)"
      contains: "describe"
    - path: "app/api/hero/route.ts"
      provides: "Read-only cache endpoint with inline relaxation comment"
      contains: "force-dynamic"
  key_links:
    - from: "lib/api/hero.ts"
      to: "lib/api/trending.ts (plan 05 — pickHeroCandidate)"
      via: "named import"
      pattern: "import.*pickHeroCandidate.*from.*@/lib/api/trending"
    - from: "lib/api/hero.ts"
      to: "CACHE_KEYS.hero"
      via: "cacheSet"
      pattern: "cacheSet.*CACHE_KEYS\\.hero"
---

<objective>
Build the cross-platform hero auto-promotion engine (REQ-hero-auto-promotion). `lib/api/hero.ts` exposes `promoteHero()` — the cron Tier 3 (plan 09) invokes it. It pulls per-platform ranked topics from cache, calls `pickHeroCandidate` (plan 05), and on a qualifying topic fuses the matching YouTube video + Reddit post + news headline into a `HeroStory` written to `CACHE_KEYS.hero`. `app/api/hero/route.ts` reads that cache (10-min TTL); cold-miss returns 503 with the widget falling back to `deriveHeroStory()` (handled in plan 10). A unit test pins the multi-source fallback chain (incl. the Reddit-only case).

Purpose: REQ-hero-auto-promotion + D3 relaxation. The inline relaxation comment in the route handler is mandatory (CON-hero-relaxation-disclosure) — future readers must not silently re-tighten to literal "#1 on all 3". Reddit posts feeding the fallback chain fixes prior-iteration WARNING 4 — REQ-hero-auto-promotion requires "fuse top video + top post + tweet count", and the previous draft fetched Reddit but ignored it.
Output: `lib/api/hero.ts`, `lib/api/hero.test.ts`, `app/api/hero/route.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-CONTEXT.md
@/Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-PATTERNS.md
@/Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/intel/constraints.md
@/Users/akshaydhaliwal/claude-code/ai-dashboard/CLAUDE.md
@/Users/akshaydhaliwal/claude-code/ai-dashboard/app/api/trending/route.ts
@/Users/akshaydhaliwal/claude-code/ai-dashboard/lib/cache/helpers.ts
@/Users/akshaydhaliwal/claude-code/ai-dashboard/lib/api/trending.ts
@/Users/akshaydhaliwal/claude-code/ai-dashboard/lib/types.ts
@/Users/akshaydhaliwal/claude-code/ai-dashboard/lib/constants.ts
@/Users/akshaydhaliwal/claude-code/ai-dashboard/components/widgets/HeroStoryCard.tsx

<interfaces>
From lib/api/trending.ts (plan 05):
  export function pickHeroCandidate(ranked: Record<"youtube"|"reddit"|"twitter", TrendingTopic[]>): TrendingTopic | null;

From lib/types.ts:
  export interface HeroStory extends TrendingTopic {
    headline: string;
    thumbnailUrl: string | null;
    link: string | null;
  }
  export interface RedditPost { id: string; title: string; url: string; subreddit: string; ... }

From lib/constants.ts (plan 03):
  CACHE_KEYS.hero, CACHE_KEYS.trendingRanked, CACHE_KEYS.youtube, CACHE_KEYS.news, CACHE_KEYS.reddit, CACHE_KEYS.twitter
  CACHE_MAX_AGE.tenMin = 10 * 60 * 1000
  CACHE_MAX_AGE.default = 15 * 60 * 1000

Trending route pattern (app/api/trending/route.ts:1-23) — mirror for hero route.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/api/hero.ts with promoteHero() — cron Tier 3 entrypoint with multi-source fallback (video + post + news)</name>
  <read_first>
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/api/trending.ts (plan 05 — `pickHeroCandidate` export + `TrendingTopic` per-platform shape)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/cache/helpers.ts (`cacheGet`, `cacheSet`)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/constants.ts (plan 03 — `CACHE_KEYS.hero`, `CACHE_MAX_AGE.tenMin`)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/types.ts (`HeroStory`, `TrendingTopic`, `Video`, `RedditPost`, `Tweet`, `NewsItem`)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/components/widgets/HeroStoryCard.tsx (lines 1-34 — `deriveHeroStory` is the shape reference; the API must produce the SAME `HeroStory` shape so the widget can render either source identically)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-CONTEXT.md (D3 — top-3-on-all-3; inline doc disclosure required)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/intel/constraints.md (CON-hero-relaxation-disclosure)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/REQUIREMENTS.md (REQ-hero-auto-promotion — explicit "fuse top video + top post + tweet count")
  </read_first>
  <acceptance_criteria>
    - `promoteHero` is exported, async, and returns `Promise<HeroStory | null>`.
    - When `pickHeroCandidate` returns null, `promoteHero` returns null WITHOUT writing to `CACHE_KEYS.hero`.
    - Headline fallback order: news.title → reddit-post.title → video.title → synthesized cross-platform string.
    - Link fallback order: news.link → reddit-post.url → video.url → null.
    - thumbnailUrl: video.thumbnailUrl when available, else null.
    - All fetches use `cacheGet` with `CACHE_MAX_AGE.default`; partial fetch failures (any source null) do not throw.
    - `pickHeroCandidate` is consumed (not reimplemented); top-3 logic does not appear in this module.
  </acceptance_criteria>
  <action>
    Create `lib/api/hero.ts`. Imports: `import { cacheGet, cacheSet } from "@/lib/cache/helpers"`, `import { CACHE_KEYS, CACHE_MAX_AGE } from "@/lib/constants"`, `import { pickHeroCandidate } from "@/lib/api/trending"` (plan 05 — DO NOT reimplement top-3 logic here), `import type { TrendingTopic, HeroStory, Video, RedditPost, Tweet, NewsItem } from "@/lib/types"`. Define a module-local helper `escapeRegex(s: string): string` mirroring `components/widgets/HeroStoryCard.tsx:4-6`.

    Export `async function promoteHero(): Promise<HeroStory | null>`. This function is invoked by the cron Tier 3 (plan 09) AFTER Tier 2 has populated the ZSETs and ranked trending. Steps:
    (a) Read ranked: `const rankedCached = await cacheGet<TrendingTopic[]>(CACHE_KEYS.trendingRanked, CACHE_MAX_AGE.tenMin); if (!rankedCached || !rankedCached.data) return null` — without ranked data, no hero is possible.
    (b) Group `rankedCached.data` into per-platform arrays by inspecting each topic's `sources` array: build `perPlatform: Record<"youtube"|"reddit"|"twitter", TrendingTopic[]> = { youtube: [], reddit: [], twitter: [] }`. For each topic row, if `topic.sources.includes("youtube")` push into `perPlatform.youtube`; same for `reddit` and `twitter`. Each per-platform array remains in velocity-descending order (inherited from plan 05's sort).
    (c) `const candidate = pickHeroCandidate(perPlatform); if (!candidate) { console.log("[hero] no top-3-on-all-3 candidate this cycle"); return null; }` — do NOT write to cache when no candidate qualifies (so `/api/hero` returns 503 via `cacheGet` cold-miss).
    (d) Fuse with the platform top items via parallel `cacheGet`s (Promise.all): videos from `CACHE_KEYS.youtube`, redditPosts from `CACHE_KEYS.reddit`, tweets from `CACHE_KEYS.twitter`, news from `CACHE_KEYS.news`. Each uses `CACHE_MAX_AGE.default`. Coerce missing caches to `null` arrays — partial fusion is acceptable.
    (e) Build the `hero: HeroStory` object. Fields from candidate: `topic`, `mentionCount`, `velocity`, `sources`, `score`. For matching: construct `const re = new RegExp(\`\\\\b\${escapeRegex(candidate.topic)}\\\\b\`, "i")`. Find `matchingNews = newsItems?.find(n => re.test(n.title)) ?? null`, `matchingPost = redditPosts?.find(p => re.test(p.title)) ?? null`, `matchingVideo = videos?.find(v => re.test(v.title)) ?? null`. (Tweets are NOT matched for headline/link content — only their count contributes to ranking, per REQ-hero-auto-promotion wording.) Build the fallback chains:
      - `headline`: `matchingNews?.title ?? matchingPost?.title ?? matchingVideo?.title ?? \`${candidate.topic} is trending across YouTube, Reddit, and X\`` (the synthesized cross-platform headline is the last resort).
      - `thumbnailUrl`: `matchingVideo?.thumbnailUrl ?? null`.
      - `link`: `matchingNews?.link ?? matchingPost?.url ?? matchingVideo?.url ?? null`.
      This three-source fallback chain (news → reddit-post → video) is the fix for prior-iteration WARNING 4. Reddit posts MUST appear in both the headline AND link fallback chains — REQ-hero-auto-promotion explicitly requires "fuse top video + top post + tweet count".
    (f) `await cacheSet(CACHE_KEYS.hero, hero)`. Return `hero`.
    Do NOT call `pickHeroCandidate` more than once per invocation. Do NOT throw — every failure path returns `null` (the cron Tier 3 expects resilient behavior). Do NOT reimplement the top-3 check inline (use plan 05's helper). Do NOT skip the Reddit-post matching step — the test in task 2 will fail if it's missing.
  </action>
  <verify>
    <automated>cd /Users/akshaydhaliwal/claude-code/ai-dashboard && npx tsc --noEmit && grep -qE '^export async function promoteHero' lib/api/hero.ts && grep -q 'pickHeroCandidate' lib/api/hero.ts && grep -q 'CACHE_KEYS.hero' lib/api/hero.ts && grep -q 'cacheSet' lib/api/hero.ts && grep -q 'matchingPost' lib/api/hero.ts && grep -q 'redditPosts' lib/api/hero.ts</automated>
  </verify>
  <done>
    File compiles. `promoteHero` exported. `pickHeroCandidate` is consumed (not reimplemented). On no-candidate, function returns null without writing to cache. `matchingPost` (Reddit post) is in both headline and link fallback chains.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write lib/api/hero.test.ts pinning the multi-source fallback chain (incl. Reddit-only case)</name>
  <read_first>
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/api/hero.ts (system under test, from task 1)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/cache/helpers.ts (boundary mock target)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/api/trending.ts (`pickHeroCandidate` — may be mocked at module boundary OR the test can rely on the real implementation with hand-rolled per-platform arrays)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/types.ts (`HeroStory`, `TrendingTopic`, `RedditPost`, `Video`, `NewsItem`)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/REQUIREMENTS.md (REQ-hero-auto-promotion — "fuse top video + top post + tweet count"; the Reddit-only-fallback case is the missing-coverage gap from prior iteration)
  </read_first>
  <acceptance_criteria>
    - At least 4 test cases pass: (1) all-three-sources present → news wins; (2) news missing + reddit present + video present → reddit-post wins headline AND link; (3) news missing + reddit missing + video present → video wins; (4) all matching items missing → synthesized headline + null link.
    - The Reddit-only-fallback test (case 2) MUST be present — this is the explicit gap from prior-iteration WARNING 4.
    - `cacheGet` is mocked at `@/lib/cache/helpers`; no real Redis access.
  </acceptance_criteria>
  <action>
    Create `lib/api/hero.test.ts`. Import `describe`, `it`, `expect`, `vi`, `beforeEach` from `"vitest"`. Mock `"@/lib/cache/helpers"` (`cacheGet: vi.fn()`, `cacheSet: vi.fn()`). Mock `"@/lib/api/trending"` so `pickHeroCandidate` returns a fixed candidate `{ topic: "Claude", mentionCount: 30, velocity: 5, sources: ["youtube","reddit","twitter"], score: 5 }` (this isolates hero-fusion behavior from the top-3 logic). Build a helper `setupCaches({ ranked, videos, posts, tweets, news })` that programs the `cacheGet` mock per-key.

    Test cases:
    (1) ALL SOURCES PRESENT — news, reddit-post, video, tweets all have items matching "Claude". Expect `hero.headline === <newsTitle>`; `hero.link === <newsLink>`; `hero.thumbnailUrl === <videoThumbnail>`. (News wins the fallback chain.)
    (2) REDDIT-ONLY FALLBACK (the missing-coverage case from prior iteration) — news cache returns `{ data: [] }` (or items with non-matching titles); reddit cache returns a post titled "Claude 4 launches with new context window"; videos cache returns a video titled "Claude 4 review" matching too. Expect `hero.headline === "Claude 4 launches with new context window"` (reddit post wins because news missed); `hero.link === <redditPost.url>`. This pins REQ-hero-auto-promotion's "top post" requirement and is the regression test for WARNING 4.
    (3) VIDEO-ONLY FALLBACK — news and reddit caches return empty/non-matching items; video cache returns a video titled "Claude 4 deep dive". Expect `hero.headline === "Claude 4 deep dive"`; `hero.link === <video.url>`; `hero.thumbnailUrl === <video.thumbnailUrl>`.
    (4) SYNTHESIZED HEADLINE — all of news/reddit/video caches return empty arrays. Expect `hero.headline.includes("trending across YouTube, Reddit, and X")`; `hero.link === null`; `hero.thumbnailUrl === null`.
    (5) NO CANDIDATE — `pickHeroCandidate` mock returns `null` for this test. Expect `promoteHero()` returns `null` AND `cacheSet` was NOT called.
    (6) NO RANKED DATA — `cacheGet(CACHE_KEYS.trendingRanked, ...)` returns `null`. Expect `promoteHero()` returns `null` immediately.

    Do NOT touch the network. Do NOT import the actual Upstash redis client.
  </action>
  <verify>
    <automated>cd /Users/akshaydhaliwal/claude-code/ai-dashboard && npx vitest run lib/api/hero.test.ts --reporter=basic 2>&1 | tee /tmp/vitest-hero.log && grep -qE '(Test Files.*1 passed|Tests.*[4-9] passed)' /tmp/vitest-hero.log</automated>
  </verify>
  <done>
    `npx vitest run lib/api/hero.test.ts` exits 0. At least 4 tests pass; the Reddit-only fallback case is among them and verifies BOTH `headline` and `link` come from the matching reddit post.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create app/api/hero/route.ts with mandatory inline relaxation comment</name>
  <read_first>
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/app/api/trending/route.ts (the EXACT pattern to mirror — lines 1-23)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/constants.ts (plan 03 — `CACHE_KEYS.hero`, `CACHE_MAX_AGE.tenMin`)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/types.ts (`HeroStory`)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-CONTEXT.md (D3 + CON-hero-relaxation-disclosure — inline doc is MANDATORY)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/intel/constraints.md (CON-hero-relaxation-disclosure — relaxation from "#1 on all 3" to "top-3 on all 3, ranked by aggregate velocity")
  </read_first>
  <acceptance_criteria>
    - Route exports `const dynamic = "force-dynamic"`.
    - GET handler returns 503 on cache miss.
    - File contains the literal strings "top-3 on all 3", "deliberate relaxation", and "SCRUM-38" in a top-of-file block comment.
  </acceptance_criteria>
  <action>
    Create `app/api/hero/route.ts` mirroring `app/api/trending/route.ts`. Imports: `import { NextResponse } from "next/server"`, `import { cacheGet } from "@/lib/cache/helpers"`, `import { CACHE_KEYS, CACHE_MAX_AGE } from "@/lib/constants"`, `import type { HeroStory } from "@/lib/types"`. Export `const dynamic = "force-dynamic"`.

    ADD AT THE TOP OF THE FILE A MANDATORY JSDOC/BLOCK COMMENT (CON-hero-relaxation-disclosure) — the literal content MUST include the strings "top-3 on all 3 platforms" AND "deliberate relaxation" AND a reference to "SCRUM-38". Suggested form (multi-line `/** ... */` block immediately above `export const dynamic`):
    "Hero eligibility threshold: a topic is hero-promoted when it appears in the top-3 on all 3 platforms (YouTube AND Reddit AND X), ranked by aggregate velocity. This is a deliberate relaxation from the SCRUM-38 ticket's literal '#1 on all 3' wording — sparse X data made strict #1 rarely fire. See D3 in .planning/phases/01-scrum-38-implementation/01-CONTEXT.md before tightening."

    Export `async function GET()`: `const cached = await cacheGet<HeroStory>(CACHE_KEYS.hero, CACHE_MAX_AGE.tenMin)`. If `!cached`, return `NextResponse.json({ data: null, cachedAt: null, stale: false, error: "No qualifying cross-platform topic" }, { status: 503 })` (cold-miss / no candidate → 503; widget falls back to `deriveHeroStory()` per "Hero-card flicker policy"). Else return `NextResponse.json({ data: cached.data, cachedAt: cached.fetchedAt, stale: cached.stale })`.
  </action>
  <verify>
    <automated>cd /Users/akshaydhaliwal/claude-code/ai-dashboard && npx tsc --noEmit && grep -q 'force-dynamic' app/api/hero/route.ts && grep -q 'CACHE_KEYS.hero' app/api/hero/route.ts && grep -q 'status: 503' app/api/hero/route.ts && grep -q 'top-3 on all 3' app/api/hero/route.ts && grep -q 'deliberate relaxation' app/api/hero/route.ts && grep -q 'SCRUM-38' app/api/hero/route.ts</automated>
  </verify>
  <done>
    Route compiles. `GET /api/hero` exists. Cold-miss returns 503. The mandatory inline relaxation comment is present with all three required string tokens.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean
- `grep -q "top-3 on all 3" app/api/hero/route.ts` succeeds
- `grep -q "deliberate relaxation" app/api/hero/route.ts` succeeds
- `grep -c "pickHeroCandidate" lib/api/hero.ts` >= 1
- `grep -c "matchingPost" lib/api/hero.ts` >= 1 (the Reddit-fallback fix from prior-iteration WARNING 4)
- `npx vitest run lib/api/hero.test.ts` exits 0 with the Reddit-only-fallback test present
- `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/api/hero` returns 503 on cold start (manual / integration check)
</verification>

<success_criteria>
- REQ-hero-auto-promotion met: top-3-on-all-3 logic via plan 05's `pickHeroCandidate`; fusion uses top video + top post + tweet count (Reddit post participates in headline + link fallback chains — fixes WARNING 4)
- Inline relaxation comment present (CON-hero-relaxation-disclosure)
- 503 on cold-miss preserves the widget's `deriveHeroStory()` fallback path
- `promoteHero()` ready for cron Tier 3 wiring in plan 09
- Unit test pins the Reddit-only-fallback case (regression coverage for the prior iteration gap)
</success_criteria>

<output>
After completion, create `.planning/phases/01-scrum-38-implementation/01-08-SUMMARY.md` per @$HOME/.claude/get-shit-done/templates/summary.md.
</output>
