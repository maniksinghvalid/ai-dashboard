---
phase: 01-scrum-38-implementation
plan: 11
type: execute
wave: 4
depends_on: ["01-01", "01-07"]
files_modified:
  - .env.example
  - CLAUDE.md
autonomous: true
requirements:
  - REQ-env-contract
must_haves:
  truths:
    - "`.env.example` contains `TOGETHER_API_KEY=` (F1 locked)"
    - "`.env.example` contains `SENTIMENT_DAILY_CHAR_BUDGET=200000` (F1 default)"
    - "`HUGGINGFACE_API_TOKEN` does NOT appear anywhere in `.env.example` or `CLAUDE.md` (F1 explicitly forbids)"
    - "`CLAUDE.md` Commands section includes `npm test`, `npm run test:watch`, `npm run test:coverage`"
    - "`CLAUDE.md` Environment Variables section lists `TOGETHER_API_KEY` and `SENTIMENT_DAILY_CHAR_BUDGET`"
  artifacts:
    - path: ".env.example"
      provides: "Updated env contract with Together AI vars"
      contains: "TOGETHER_API_KEY"
    - path: "CLAUDE.md"
      provides: "Updated Commands + Environment Variables sections"
      contains: "TOGETHER_API_KEY"
  key_links:
    - from: ".env.example"
      to: "CLAUDE.md Environment Variables section"
      via: "matching variable names in both"
      pattern: "TOGETHER_API_KEY"
---

<objective>
Migrate the env contract to Together AI (REQ-env-contract / F1). Add `TOGETHER_API_KEY` and `SENTIMENT_DAILY_CHAR_BUDGET` to `.env.example`; confirm `HUGGINGFACE_API_TOKEN` is NOT present anywhere; update `CLAUDE.md` Commands section to advertise `npm test` (test framework added in plan 01) and Environment Variables section to reflect the new Together AI vars.

Purpose: REQ-env-contract. Closes ROADMAP success criterion 8. Final paperwork plan — no runtime code changes.
Output: Updated `.env.example`, updated `CLAUDE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-CONTEXT.md
@/Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-PATTERNS.md
@/Users/akshaydhaliwal/claude-code/ai-dashboard/.env.example
@/Users/akshaydhaliwal/claude-code/ai-dashboard/CLAUDE.md
@/Users/akshaydhaliwal/claude-code/ai-dashboard/package.json

<interfaces>
Current .env.example (14 lines) — see file. Required entries are present.

Current CLAUDE.md "Commands" section:
  - `npm run dev` — start dev server (localhost:3000)
  - `npm run build` — production build
  - `npm run lint` — ESLint with Prettier compatibility
  - No test framework configured yet  ← UPDATE THIS LINE

Current CLAUDE.md "Environment Variables" section:
  See `.env.example` for all variables. Required: `YOUTUBE_API_KEY`, ..., `CRON_SECRET`, `NEXT_PUBLIC_SENTRY_DSN`, `SENTRY_AUTH_TOKEN`. Optional: ...
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TOGETHER_API_KEY and SENTIMENT_DAILY_CHAR_BUDGET to .env.example</name>
  <read_first>
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.env.example (full file — 14 lines; no `HUGGINGFACE_API_TOKEN` currently present, verify)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-CONTEXT.md (F1 locked: `TOGETHER_API_KEY` + `SENTIMENT_DAILY_CHAR_BUDGET` default 200000; `HUGGINGFACE_API_TOKEN` FORBIDDEN)
  </read_first>
  <action>
    Append two new lines to `.env.example` at the bottom (after `NEXT_PUBLIC_SENTRY_TRACES_SAMPLE_RATE=1`): `TOGETHER_API_KEY=` (no value — placeholder), `SENTIMENT_DAILY_CHAR_BUDGET=200000` (default value per F1). Do NOT add `HUGGINGFACE_API_TOKEN` anywhere. Do NOT remove or modify any existing entry. Preserve trailing newline. Verify after writing that `grep -c HUGGINGFACE .env.example` returns 0.
  </action>
  <verify>
    <automated>cd /Users/akshaydhaliwal/claude-code/ai-dashboard && grep -q '^TOGETHER_API_KEY=$' .env.example && grep -q '^SENTIMENT_DAILY_CHAR_BUDGET=200000$' .env.example && [ "$(grep -c HUGGINGFACE .env.example)" = "0" ]</automated>
  </verify>
  <done>
    `.env.example` has both new entries with exact strings. `HUGGINGFACE_API_TOKEN` is not present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md Commands + Environment Variables sections</name>
  <read_first>
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/CLAUDE.md (full file — Commands section near top; Environment Variables section near bottom)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/package.json (post-plan-01 — scripts `test`, `test:watch`, `test:coverage` exist)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-CONTEXT.md (REQ-env-contract acceptance: CLAUDE.md "Environment Variables" section reflects the change; D6 / REQ-vitest-bootstrap: "Commands" section must add `npm test`)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-PATTERNS.md ("CLAUDE.md" section)
  </read_first>
  <action>
    Edit `CLAUDE.md`:
    
    SECTION 1 — "Commands" (near the top of the file). REPLACE the existing line `- No test framework configured yet` with three new bullets in the same style:
    - `- \`npm test\` — run Vitest unit tests (pure-function core)`
    - `- \`npm run test:watch\` — Vitest in watch mode`
    - `- \`npm run test:coverage\` — Vitest with v8 coverage`
    Preserve all four existing `npm run dev|build|lint` bullets.
    
    SECTION 2 — "Environment Variables" (near the bottom of the file). The current paragraph lists Required and Optional vars. Update it to ADD `TOGETHER_API_KEY` to the Required list (sentiment engine fails closed without it) and `SENTIMENT_DAILY_CHAR_BUDGET` to the Optional list with a note "(default 200000)". Ensure `HUGGINGFACE_API_TOKEN` is NOT mentioned anywhere — neither as Required nor as Optional nor as deprecated. Final sentence may keep its existing closing trace-sample-rate optionals.
    
    Do NOT touch any other section of `CLAUDE.md` (Architecture, Key Patterns, Git Workflow, Deployment, etc.).
  </action>
  <verify>
    <automated>cd /Users/akshaydhaliwal/claude-code/ai-dashboard && grep -q 'npm test' CLAUDE.md && grep -q 'TOGETHER_API_KEY' CLAUDE.md && grep -q 'SENTIMENT_DAILY_CHAR_BUDGET' CLAUDE.md && [ "$(grep -c HUGGINGFACE CLAUDE.md)" = "0" ] && ! grep -q 'No test framework configured yet' CLAUDE.md</automated>
  </verify>
  <done>
    Commands section advertises `npm test` (plus watch + coverage). Environment Variables section lists `TOGETHER_API_KEY` (Required) and `SENTIMENT_DAILY_CHAR_BUDGET` (Optional, default 200000). `HUGGINGFACE_API_TOKEN` is absent. The stale `No test framework configured yet` line is gone.
  </done>
</task>

</tasks>

<verification>
- `grep -r HUGGINGFACE .env.example CLAUDE.md` returns nothing (acceptance criterion 8 verbatim, ROADMAP)
- `grep -c TOGETHER_API_KEY .env.example CLAUDE.md | grep -v ':0$' | wc -l` == 2 (both files mention it)
- `grep -c SENTIMENT_DAILY_CHAR_BUDGET .env.example CLAUDE.md | grep -v ':0$' | wc -l` == 2
- `grep -c 'npm test' CLAUDE.md` >= 1
- `grep -c 'No test framework configured yet' CLAUDE.md` == 0
</verification>

<success_criteria>
- REQ-env-contract met: both new env vars present in `.env.example` with the locked defaults
- `HUGGINGFACE_API_TOKEN` does not appear anywhere (F1 enforcement)
- `npm test` is advertised in CLAUDE.md Commands section
- ROADMAP success criterion 8 closed
</success_criteria>

<output>
After completion, create `.planning/phases/01-scrum-38-implementation/01-11-SUMMARY.md` per @$HOME/.claude/get-shit-done/templates/summary.md.
</output>
