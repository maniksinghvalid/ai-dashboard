---
phase: 01-scrum-38-implementation
plan: 09
type: execute
wave: 3
depends_on: ["01-05", "01-06", "01-07", "01-08"]
files_modified:
  - app/api/cron/refresh/route.ts
autonomous: true
requirements:
  - REQ-cron-three-tier-dag
must_haves:
  truths:
    - "`app/api/cron/refresh/route.ts` exports `const maxDuration = 30` (F3 / CON-vercel-max-duration locked)"
    - "Refresh runs in three sequenced tiers; each tier wrapped in `Promise.allSettled`"
    - "Tier 1: youtube + reddit + twitter + news fetches in parallel"
    - "Tier 2: `fetchAndCacheTrending(videos, posts, tweets)` writes ZSETs + ranked cache (depends on Tier 1)"
    - "Tier 3: `promoteHero()` + `writeSpikeAlertsFromTrending(getAlertsRows(now))` + `fetchAndCacheSentiment(items)` in parallel (depend on Tier 2)"
    - "Alerts rows come from plan 05's canonical `getAlertsRows(now)` — NEVER a manually reconstructed row shape from the trending output (fixes prior-iteration BLOCKER 1+2)"
    - "Failure of any leg does NOT block siblings or downstream-tier siblings where they are independent"
    - "Per-leg Sentry.captureException is preserved + extended to the new legs (guarded by NEXT_PUBLIC_SENTRY_DSN)"
    - "GET (Bearer-cron) + POST (QStash) entry points remain unchanged"
  artifacts:
    - path: "app/api/cron/refresh/route.ts"
      provides: "3-tier DAG orchestrator with maxDuration=30"
      contains: "export const maxDuration = 30"
  key_links:
    - from: "app/api/cron/refresh/route.ts"
      to: "Tier 3 legs"
      via: "promoteHero + writeSpikeAlertsFromTrending(getAlertsRows(now)) + fetchAndCacheSentiment"
      pattern: "Promise.allSettled.*promoteHero"
---

<objective>
Restructure `app/api/cron/refresh/route.ts` into the locked 3-tier DAG (D4 / CON-cron-dag-tiers). Add `export const maxDuration = 30` (F3 / CON-vercel-max-duration). Tier 1 is the existing external fetch fan-out (preserved verbatim — pattern at lines 22-28). Tier 2 is `fetchAndCacheTrending` writing ZSETs (depends on Tier 1's fulfilled values). Tier 3 is three parallel legs: `promoteHero()` (plan 08), `writeSpikeAlertsFromTrending(getAlertsRows(now))` (plan 06 + plan 05), `fetchAndCacheSentiment(items)` (plan 07). The alerts leg consumes plan 05's canonical `getAlertsRows(now)` helper directly — no manual row reconstruction.

Purpose: REQ-cron-three-tier-dag. This is the orchestration layer that activates plans 05-08 as a cohesive refresh cycle. ~12s expected under load; 30s ceiling gives headroom. Consuming `getAlertsRows(now)` fixes prior-iteration BLOCKER 1+2: the previous draft reconstructed `topicId` from `topic.topic.toLowerCase().replace(/\s+/g, "-")` (incoherent for topics whose label differs from id) AND passed cross-platform aggregate velocity (incoherent against plan 06's per-platform 24h baseline).
Output: Refactored `app/api/cron/refresh/route.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-CONTEXT.md
@/Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-PATTERNS.md
@/Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/intel/constraints.md
@/Users/akshaydhaliwal/claude-code/ai-dashboard/CLAUDE.md
@/Users/akshaydhaliwal/claude-code/ai-dashboard/app/api/cron/refresh/route.ts
@/Users/akshaydhaliwal/claude-code/ai-dashboard/lib/api/trending.ts
@/Users/akshaydhaliwal/claude-code/ai-dashboard/lib/api/hero.ts
@/Users/akshaydhaliwal/claude-code/ai-dashboard/lib/api/alerts.ts
@/Users/akshaydhaliwal/claude-code/ai-dashboard/lib/api/sentiment.ts

<interfaces>
From lib/api/trending.ts (plan 05):
  export async function fetchAndCacheTrending(videos, posts, tweets): Promise<TrendingTopic[]>;
  export async function getAlertsRows(now: number): Promise<Array<{ topicId: string; topic: string; platform: "youtube"|"reddit"|"twitter"; velocity: number }>>;
  // getAlertsRows emits CANONICAL rows: topicId === AI_TOPICS[k].id (NOT a label-derived slug); velocity is per-platform (NOT cross-platform aggregate).

From lib/api/hero.ts (plan 08):
  export async function promoteHero(): Promise<HeroStory | null>;

From lib/api/alerts.ts (plan 06):
  export async function writeSpikeAlertsFromTrending(rows: Array<{ topicId; topic; platform; velocity }>): Promise<void>;

From lib/api/sentiment.ts (plan 07):
  export async function fetchAndCacheSentiment(items: Array<{ text: string }>): Promise<Sentiment>;

Existing pattern (app/api/cron/refresh/route.ts:13-90) — preserve Tier-1 shape + per-leg Sentry capture (lines 38-41).
Existing GET (Bearer) + POST (QStash) handlers (lines 92-135) — UNCHANGED.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add maxDuration export and refactor refreshAllFeeds into 3-tier DAG (alerts leg consumes canonical getAlertsRows)</name>
  <read_first>
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/app/api/cron/refresh/route.ts (full file — Tier-1 pattern at 22-28; per-leg Sentry capture at 38-41; GET/POST handlers at 92-135 MUST remain unchanged)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/api/trending.ts (plan 05 — `fetchAndCacheTrending` signature unchanged; `getAlertsRows(now)` is the canonical source-of-truth row helper that this plan consumes)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/api/hero.ts (plan 08 — `promoteHero()`)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/api/alerts.ts (plan 06 — `writeSpikeAlertsFromTrending(rows)`; rows MUST match the canonical shape from plan 05's `getAlertsRows`)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/lib/api/sentiment.ts (plan 07 — `fetchAndCacheSentiment(items)`)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/intel/constraints.md (CON-vercel-max-duration, CON-cron-dag-tiers — locked)
    - /Users/akshaydhaliwal/claude-code/ai-dashboard/.planning/phases/01-scrum-38-implementation/01-CONTEXT.md (D4 / F3)
  </read_first>
  <acceptance_criteria>
    - `export const maxDuration = 30` appears exactly once.
    - `Promise.allSettled` appears at least 3 times (once per tier).
    - Tier-3 alerts leg invokes `writeSpikeAlertsFromTrending(await getAlertsRows(now))` (or assigns to a local first) — NEVER constructs rows from `topic.topic.toLowerCase().replace(...)` or similar inline derivation.
    - `summary` object has keys `youtube`, `reddit`, `twitter`, `news`, `trending`, `hero`, `alerts`, `sentiment` (each `"ok" | "failed"`).
    - GET (Bearer auth) and POST (QStash) handlers are byte-for-byte unchanged.
  </acceptance_criteria>
  <action>
    Refactor `app/api/cron/refresh/route.ts` in place.

    STEP A — At the top of the file (immediately after `export const dynamic = "force-dynamic"`), add `export const maxDuration = 30;` (F3 / CON-vercel-max-duration locked).

    STEP B — Add imports for the new legs: `import { promoteHero } from "@/lib/api/hero"`, `import { writeSpikeAlertsFromTrending } from "@/lib/api/alerts"`, `import { fetchAndCacheSentiment } from "@/lib/api/sentiment"`. Extend the existing `fetchAndCacheTrending` import to ALSO pull `getAlertsRows` from `@/lib/api/trending` (plan 05 exports both): `import { fetchAndCacheTrending, getAlertsRows } from "@/lib/api/trending"`.

    STEP C — Inside `refreshAllFeeds()`, restructure into the 3-tier DAG (CON-cron-dag-tiers):

    Tier 1 (existing fan-out — keep verbatim; this is locked as the canonical analog): `Promise.allSettled([fetchYouTubeVideos(), fetchRedditPosts(), fetchTweets(), fetchNews()])`. Preserve the existing per-leg Sentry capture pattern (lines 34-71) and the existing `summary.youtube|reddit|twitter|news = "ok" | "failed"` accounting.

    Tier 2 (depends on Tier 1 fulfilled values): wrap `fetchAndCacheTrending(videos, posts, tweets)` in `Promise.allSettled([...])` (array with one entry — keeps the per-tier shape uniform per D4). On success, do NOT need to capture the returned `TrendingTopic[]` — Tier 3's alerts leg reads canonical rows directly via `getAlertsRows(now)`, which reads the ZSETs that `fetchAndCacheTrending` just populated. On failure, `console.error("[cron] Tier 2 trending failed:", reason); Sentry.captureException(reason)` (guarded by `NEXT_PUBLIC_SENTRY_DSN`); set `summary.trending = "failed"` and proceed to Tier 3 anyway — `promoteHero` reads from cache and `getAlertsRows` returns rows from the (possibly stale) ZSETs, so a Tier-2 failure means downstream legs likely no-op cleanly but they must not be blocked.

    Tier 3 (parallel, all depend on Tier 2): Compute the inputs FIRST, then dispatch:
    - `const now = Math.floor(Date.now() / 1000);`
    - `const rowsForAlerts = await getAlertsRows(now);` — CANONICAL rows from plan 05. This replaces the prior-iteration manual reconstruction (`topic.topic.toLowerCase().replace(/\s+/g, "-")`) which broke for topics whose label differs from id AND wrongly used cross-platform aggregate velocity. `getAlertsRows` emits per-(topic, platform) rows with literal `AI_TOPICS[k].id` and per-platform velocity — exactly what plan 06's `computeBaseline24h` expects. If `getAlertsRows` throws (e.g., Tier 2 failed and ZSETs are unreachable), catch and fall back to `[]` so the alerts leg no-ops cleanly. DO NOT reintroduce any inline slug derivation here.
    - `itemsForSentiment`: build from Tier-1 outputs. `const items = [...posts.map(p => ({ text: p.title })), ...tweets.map(t => ({ text: t.text }))]` (Reddit titles + tweets per REQ-sentiment-engine description; YouTube titles are intentionally excluded to keep batch size manageable within the 5s budget — Claude's discretion per CONTEXT line 110). If Tier 1 produced zero items, pass `[]` — the engine will no-op via its empty-batch path.
    - Then dispatch: `await Promise.allSettled([promoteHero(), writeSpikeAlertsFromTrending(rowsForAlerts), fetchAndCacheSentiment(itemsForSentiment)])`.

    For each Tier-3 leg, on rejection: `console.error("[cron] Tier 3 <leg> failed:", reason); if (process.env.NEXT_PUBLIC_SENTRY_DSN) Sentry.captureException(reason);`. Update `summary` with new keys: `summary.hero`, `summary.alerts`, `summary.sentiment` (each `"ok" | "failed"`). Initialize these in the `summary` object at the top of `refreshAllFeeds()`.

    STEP D — Return the existing `NextResponse.json({ status: "ok", refreshedAt: new Date().toISOString(), summary })` shape.

    DO NOT touch the GET (Bearer) or POST (QStash) handlers (lines 92-135). DO NOT remove the existing per-leg `Sentry.captureException` calls. DO NOT call any Tier-3 leg INSIDE the Tier-2 try/catch. DO NOT change the order of tiers (Tier 1 → Tier 2 → Tier 3 is locked by CON-cron-dag-tiers). DO NOT reintroduce `topic.topic.toLowerCase().replace(...)` or any other inline `topicId` derivation — `getAlertsRows` is the SOLE source of canonical rows.
  </action>
  <verify>
    <automated>cd /Users/akshaydhaliwal/claude-code/ai-dashboard && npx tsc --noEmit && npx next lint --quiet 2>&1 | tee /tmp/lint.log; grep -q '^export const maxDuration = 30' app/api/cron/refresh/route.ts && grep -cE 'Promise\.allSettled' app/api/cron/refresh/route.ts | awk '$1 >= 3 {exit 0} {exit 1}' && grep -q 'promoteHero' app/api/cron/refresh/route.ts && grep -q 'writeSpikeAlertsFromTrending' app/api/cron/refresh/route.ts && grep -q 'fetchAndCacheSentiment' app/api/cron/refresh/route.ts && grep -q 'getAlertsRows' app/api/cron/refresh/route.ts && grep -q 'summary.hero' app/api/cron/refresh/route.ts && grep -q 'summary.alerts' app/api/cron/refresh/route.ts && grep -q 'summary.sentiment' app/api/cron/refresh/route.ts && ! grep -E 'toLowerCase\(\)\.replace\(/\\\\s\+/g' app/api/cron/refresh/route.ts</automated>
  </verify>
  <done>
    Cron route compiles + lints. `maxDuration = 30` exported. `Promise.allSettled` appears at least 3 times (once per tier). All three new Tier-3 legs are wired and reflected in `summary`. Alerts leg consumes `getAlertsRows(now)` — no inline slug reconstruction. GET + POST handlers untouched. A manual `curl -H "Authorization: Bearer $CRON_SECRET" http://localhost:3000/api/cron/refresh` should return `summary` with all 8 keys present.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean
- `npx next lint --quiet app/api/cron/refresh/route.ts` clean
- `grep -c "Promise.allSettled" app/api/cron/refresh/route.ts` >= 3 (one per tier)
- `grep -c "export const maxDuration = 30" app/api/cron/refresh/route.ts` == 1
- `grep -c "getAlertsRows" app/api/cron/refresh/route.ts` >= 1 (canonical alerts-row consumer; fixes prior-iteration BLOCKER 1+2)
- `grep -c "toLowerCase().replace" app/api/cron/refresh/route.ts` == 0 (no manual slug derivation)
- All four imports for plan 05/06/07/08 functions present
- Manual: `curl -H "Authorization: Bearer $CRON_SECRET" http://localhost:3000/api/cron/refresh` completes < 30s and returns `summary` with keys `youtube, reddit, twitter, news, trending, hero, alerts, sentiment`
</verification>

<success_criteria>
- REQ-cron-three-tier-dag met: 3 tiers, each `Promise.allSettled`, `maxDuration = 30`
- Failure isolation preserved (existing Sentry pattern extended)
- ROADMAP success criterion 6 met (DAG runs under 30s with three new Tier-3 legs)
- Alerts leg consumes the canonical `getAlertsRows(now)` source-of-truth from plan 05 (fixes prior-iteration BLOCKER 1+2)
- Plans 10/11 can proceed once cron is wired
</success_criteria>

<output>
After completion, create `.planning/phases/01-scrum-38-implementation/01-09-SUMMARY.md` per @$HOME/.claude/get-shit-done/templates/summary.md.
</output>
