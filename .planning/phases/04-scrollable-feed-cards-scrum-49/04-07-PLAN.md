---
phase: 04-scrollable-feed-cards-scrum-49
plan: 07
type: execute
wave: 2
depends_on:
  - 02
  - 03
files_modified:
  - components/widgets/NewsWidget.tsx
  - components/widgets/__tests__/NewsWidget.test.tsx
autonomous: true
requirements:
  - D1
  - D9
  - SC-1
  - SC-6
  - D7

must_haves:
  truths:
    - "NewsWidget renders at most MAX_FEED_ITEMS (15) news rows when given more than 15 items"
    - "When given N items with 1 <= N <= 15, the badge text equals `${N} new` (D9 — live array length preserved)"
    - "The widget passes scrollable + maxBodyHeight='max-h-[320px]' to WidgetCard on the populated branch"
    - "lib/api/news.ts is not modified by this plan"
  artifacts:
    - path: "components/widgets/NewsWidget.tsx"
      provides: "Updated slice + WidgetCard wiring"
      contains: "MAX_FEED_ITEMS"
    - path: "components/widgets/__tests__/NewsWidget.test.tsx"
      provides: "Cap + badge render-time tests"
      exports: ["describe", "it"]
  key_links:
    - from: "components/widgets/NewsWidget.tsx"
      to: "lib/constants.ts"
      via: "import { MAX_FEED_ITEMS }"
      pattern: "import.*MAX_FEED_ITEMS"
---

<objective>
Rewire `NewsWidget.tsx` to slice at `MAX_FEED_ITEMS` (was `5`) and pass `scrollable maxBodyHeight="max-h-[320px]"` to WidgetCard on the populated branch. Co-author the test file proving the cap and badge contract (D9).

Purpose: One of four parallel Wave 2 rewires.

Output: Updated `NewsWidget.tsx` and new `components/widgets/__tests__/NewsWidget.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-scrollable-feed-cards-scrum-49/04-CONTEXT.md
@.planning/phases/04-scrollable-feed-cards-scrum-49/04-UI-SPEC.md
@.planning/phases/04-scrollable-feed-cards-scrum-49/04-VALIDATION.md
@CLAUDE.md
@components/widgets/NewsWidget.tsx
@lib/types.ts

<interfaces>
<!-- Current shape: NewsWidget({ items, stale, isLoading, error }: { items: NewsItem[] | null; stale: boolean; isLoading: boolean; error: Error | null }) -->
<!-- Current cap at line 87: items.slice(0, 5) -->
<!-- Existing badge: badge={count > 0 ? `${count} new` : undefined} where count = items?.length ?? 0 -->
<!-- D9 preserves live array length -->
<!-- key prop: `${item.link}-${i}` (preserve verbatim) -->

<!-- NewsItem type: title, link, source, publishedAt, summary -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author NewsWidget.test.tsx (failing)</name>
  <files>components/widgets/__tests__/NewsWidget.test.tsx</files>
  <read_first>
    - components/widgets/NewsWidget.tsx (current 94 lines)
    - lib/types.ts (NewsItem interface)
    - .planning/phases/04-scrollable-feed-cards-scrum-49/04-CONTEXT.md (D1, D9)
    - components/widgets/__tests__/WidgetCard.test.tsx (jsdom + RTL pattern)
  </read_first>
  <behavior>
    - Test 1 (D1 cap to 15): given 30 fabricated `NewsItem[]` items, the widget renders exactly 15 `<a>` rows.
    - Test 2 (D9 badge text — happy path): given 8 items, the rendered text contains `8 new`.
    - Test 3 (D9 badge text — exactly at cap): given 15 items, the rendered text contains `15 new`.
    - Test 4 (scrollable wiring on populated branch): given 5 items, a `role="region"` element with `aria-label="AI News feed, scrollable"` is present.
    - Test 5 (empty branch — no scrollable): given `items={[]}`, no `role="region"`; text `No news available` is rendered.
  </behavior>
  <action>
    Create the file `components/widgets/__tests__/NewsWidget.test.tsx`. First line MUST be `// @vitest-environment jsdom`. Standard imports.

    Define `makeNews(n: number): NewsItem[]` with required fields (title `News ${i}`, link `https://example.com/news-${i}`, source `TechCrunch`, publishedAt `new Date().toISOString()`, summary `Summary ${i}`).

    Use `afterEach(cleanup)`. Render with `<NewsWidget items={...} stale={false} isLoading={false} error={null} />`.
  </action>
  <verify>
    <automated>npx vitest run components/widgets/__tests__/NewsWidget.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    Test file exists. Runs RED against un-modified NewsWidget (current cap is 5, scrollable wiring absent).
  </done>
  <acceptance_criteria>
    - File `components/widgets/__tests__/NewsWidget.test.tsx` exists
    - First line is exactly `// @vitest-environment jsdom`
    - `grep -c 'getAllByRole("link")' components/widgets/__tests__/NewsWidget.test.tsx` is ≥ 1
    - `grep -c '"AI News feed, scrollable"' components/widgets/__tests__/NewsWidget.test.tsx` is ≥ 1
    - `grep -c 'No news available' components/widgets/__tests__/NewsWidget.test.tsx` is ≥ 1
    - `npx vitest run components/widgets/__tests__/NewsWidget.test.tsx` exits with code != 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire NewsWidget.tsx (GREEN)</name>
  <files>components/widgets/NewsWidget.tsx</files>
  <read_first>
    - components/widgets/NewsWidget.tsx (current shape)
    - components/widgets/__tests__/NewsWidget.test.tsx (the Task 1 tests)
    - lib/constants.ts (confirm MAX_FEED_ITEMS exists)
  </read_first>
  <behavior>
    The rewired component passes all five Task-1 tests and does not modify `lib/api/news.ts`.
  </behavior>
  <action>
    Edit `components/widgets/NewsWidget.tsx`:

    1. Add a new import line near the top: `import { MAX_FEED_ITEMS } from "@/lib/constants";`. Place it after the existing `@/lib/types` import.

    2. On the line `{items.slice(0, 5).map((item, i) => (` (currently line 87), change `5` to `MAX_FEED_ITEMS`. Preserve the existing `(item, i) =>` signature and the `key={`${item.link}-${i}`}` prop.

    3. On the WidgetCard call (currently `<WidgetCard icon="📰" iconBg="#0ea5e9" title="AI News" badge={count > 0 ? `${count} new` : undefined} stale={stale}>`), add `scrollable` + `maxBodyHeight` conditioned on the populated branch:

       `scrollable={Array.isArray(items) && items.length > 0 && !isLoading && !error ? true : undefined}`
       `maxBodyHeight={Array.isArray(items) && items.length > 0 && !isLoading && !error ? "max-h-[320px]" : undefined}`

    4. Do NOT modify `lib/api/news.ts`. Do NOT change the badge expression — D9 preserves the live array length.
  </action>
  <verify>
    <automated>npx vitest run components/widgets/__tests__/NewsWidget.test.tsx && npx tsc --noEmit && npm run lint -- --quiet</automated>
  </verify>
  <done>
    All 5 NewsWidget tests pass. `npx tsc --noEmit` clean. `npm run lint` clean.
  </done>
  <acceptance_criteria>
    - `npx vitest run components/widgets/__tests__/NewsWidget.test.tsx` exits 0
    - `grep -c 'items.slice(0, MAX_FEED_ITEMS)' components/widgets/NewsWidget.tsx` returns 1
    - `grep -c 'items.slice(0, 5)' components/widgets/NewsWidget.tsx` returns 0
    - `grep -cE 'import.*MAX_FEED_ITEMS.*@/lib/constants' components/widgets/NewsWidget.tsx` returns 1
    - `grep -c 'maxBodyHeight=' components/widgets/NewsWidget.tsx` returns 1
    - `grep -c 'scrollable=' components/widgets/NewsWidget.tsx` returns 1
    - `grep -c '`${count} new`' components/widgets/NewsWidget.tsx` returns 1 (D9 preserved)
    - `npm test` exits 0
    - `npx tsc --noEmit` exits 0
    - `npm run lint` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-supply-chain | Tampering | n/a | n/a | No new dependencies. |
</threat_model>

<verification>
- `npx vitest run components/widgets/__tests__/NewsWidget.test.tsx` exits 0 (5 tests)
- `lib/api/news.ts` byte-unchanged
- Full suite green
- D1 + D3 + D9 satisfied
</verification>

<success_criteria>
- 15-item cap via `MAX_FEED_ITEMS`
- Scrollable wiring on populated branch
- Badge preservation (D9)
- Layer discipline: zero changes to `lib/api/news.ts`
</success_criteria>

<output>
After completion, create `.planning/phases/04-scrollable-feed-cards-scrum-49/04-07-SUMMARY.md` capturing:
- NewsWidget.tsx diff
- Test count (5 new)
- `git diff --stat lib/api/news.ts` confirmation (empty)
</output>
