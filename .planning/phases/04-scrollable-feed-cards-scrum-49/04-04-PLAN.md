---
phase: 04-scrollable-feed-cards-scrum-49
plan: 04
type: execute
wave: 2
depends_on:
  - 02
  - 03
files_modified:
  - components/widgets/YouTubeWidget.tsx
  - components/widgets/__tests__/YouTubeWidget.test.tsx
autonomous: true
requirements:
  - D1
  - D9
  - SC-1
  - SC-6
  - D7

must_haves:
  truths:
    - "YouTubeWidget renders at most MAX_FEED_ITEMS (15) video rows when given more than 15 videos"
    - "When given N videos with 1 <= N <= 15, the badge text equals `${N} new` (D9 — live array length preserved)"
    - "The widget passes scrollable + maxBodyHeight='max-h-[320px]' to WidgetCard on the populated branch"
    - "The loading / error / empty branches do NOT pass scrollable to WidgetCard (per UI-SPEC 'Notes on the loading / error / empty branches')"
    - "lib/api/youtube.ts is not modified by this plan"
  artifacts:
    - path: "components/widgets/YouTubeWidget.tsx"
      provides: "Updated slice + WidgetCard wiring"
      contains: "MAX_FEED_ITEMS"
    - path: "components/widgets/__tests__/YouTubeWidget.test.tsx"
      provides: "Cap + badge render-time tests"
      exports: ["describe", "it"]
  key_links:
    - from: "components/widgets/YouTubeWidget.tsx"
      to: "lib/constants.ts"
      via: "import { MAX_FEED_ITEMS }"
      pattern: "import.*MAX_FEED_ITEMS"
    - from: "components/widgets/YouTubeWidget.tsx"
      to: "components/widgets/WidgetCard.tsx"
      via: "scrollable + maxBodyHeight props"
      pattern: "scrollable\\s+maxBodyHeight"
---

<objective>
Rewire `YouTubeWidget.tsx` to (a) slice videos at `MAX_FEED_ITEMS` instead of the hard-coded `4`, and (b) pass `scrollable maxBodyHeight="max-h-[320px]"` to `WidgetCard` on the populated branch. Co-author the test file proving the cap and the badge-preservation contract (D9).

Purpose: This is one of four Wave 2 rewires (the others are RedditWidget, XFeedWidget, NewsWidget — Plans 05–07). All four are parallelizable since they touch disjoint files.

Output: Updated `YouTubeWidget.tsx` and new `components/widgets/__tests__/YouTubeWidget.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-scrollable-feed-cards-scrum-49/04-CONTEXT.md
@.planning/phases/04-scrollable-feed-cards-scrum-49/04-UI-SPEC.md
@.planning/phases/04-scrollable-feed-cards-scrum-49/04-VALIDATION.md
@CLAUDE.md
@components/widgets/YouTubeWidget.tsx
@lib/types.ts

<interfaces>
<!-- Current shape: YouTubeWidget({ videos, stale, isLoading, error }: { videos: Video[] | null; stale: boolean; isLoading: boolean; error: Error | null }) -->
<!-- Current cap at line 82: videos.slice(0, 4) -->
<!-- Existing badge logic at line 67: badge={count > 0 ? `${count} new` : undefined} where count = videos?.length ?? 0 -->
<!-- D9 preserves the badge as live array length (1-15). -->

<!-- WidgetCard contract (post Plan 03): -->
<!--   scrollable?: boolean; maxBodyHeight?: string  (both optional, default behavior unchanged) -->

<!-- Video type (from lib/types.ts): id, title, channelName, channelId, thumbnailUrl, viewCount, publishedAt, viewVelocity, url -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author YouTubeWidget.test.tsx (failing)</name>
  <files>components/widgets/__tests__/YouTubeWidget.test.tsx</files>
  <read_first>
    - components/widgets/YouTubeWidget.tsx (current shape — confirm props + slice line)
    - lib/types.ts (Video interface)
    - .planning/phases/04-scrollable-feed-cards-scrum-49/04-UI-SPEC.md (Per-Widget Visual Rules + Loading/Error/Empty notes)
    - .planning/phases/04-scrollable-feed-cards-scrum-49/04-CONTEXT.md (D1, D9)
    - components/widgets/__tests__/WidgetCard.test.tsx (Plan 03 — for the testing-library import + jsdom pragma pattern)
  </read_first>
  <behavior>
    - Test 1 (D1 cap to 15): given 25 fabricated `Video[]` items, the widget renders exactly 15 `<a>` elements (one per video item). Assert via `screen.getAllByRole("link").length === 15`.
    - Test 2 (D9 badge text — happy path): given 7 videos, the rendered text content contains `7 new`. Assert by `screen.getByText("7 new")` resolves.
    - Test 3 (D9 badge text — exactly at the cap): given 15 videos, the rendered text contains `15 new` (NOT `15 of 25` or similar — the badge reads live array length, not post-cap count; D9 preserves this even though it equals the cap when N=15).
    - Test 4 (scrollable wiring on populated branch): given 5 videos (overflowing is irrelevant for this assertion), the rendered DOM contains a `role="region"` element with `aria-label="YouTube feed, scrollable"`. This proves YouTubeWidget passes `scrollable` to WidgetCard.
    - Test 5 (empty branch): given an empty videos array (`[]`), no `role="region"` element is rendered (scrollable is NOT passed in the empty branch — UI-SPEC "Loading / error / empty branches"). The empty-state message `No videos available` is rendered.
  </behavior>
  <action>
    Create the file `components/widgets/__tests__/YouTubeWidget.test.tsx`. First line MUST be `// @vitest-environment jsdom`. Import `describe`, `it`, `expect`, `afterEach` from `"vitest"`; `render`, `screen`, `cleanup` from `"@testing-library/react"`; `YouTubeWidget` from `"@/components/widgets/YouTubeWidget"`; `type Video` from `"@/lib/types"`.

    Define a `makeVideos(n: number): Video[]` helper that fabricates Video objects with required fields (id `v${i}`, title `Video ${i}`, channelName `ch`, channelId `c`, thumbnailUrl empty string `""`, viewCount 1000, publishedAt `new Date().toISOString()`, viewVelocity 0, url `https://youtu.be/v${i}`). Empty `thumbnailUrl` avoids `next/image` jsdom complications — the existing component renders the play-icon overlay regardless.

    Render with the canonical props: `<YouTubeWidget videos={...} stale={false} isLoading={false} error={null} />`. Use `afterEach(cleanup)`.

    For Test 5, render with `videos={[]}`. Assert `screen.queryByRole("region")` is null and `screen.getByText("No videos available")` resolves.

    Do NOT mock next/image. Empty `thumbnailUrl` makes the conditional `video.thumbnailUrl ? <Image .../> : null` skip the `<Image>` element entirely, so jsdom is unaffected.
  </action>
  <verify>
    <automated>npx vitest run components/widgets/__tests__/YouTubeWidget.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    The test file exists. Running it against the un-modified `YouTubeWidget.tsx` produces **FAIL** for Test 1 (current cap is 4, not 15), Test 3 (15-row render asserts but cap is 4), and Test 4 (no `scrollable` passed today, so no `role="region"`). Tests 2 and 5 may pass against today's code — that is fine for RED state.
  </done>
  <acceptance_criteria>
    - File `components/widgets/__tests__/YouTubeWidget.test.tsx` exists
    - First line is exactly `// @vitest-environment jsdom`
    - `grep -c 'getAllByRole("link")' components/widgets/__tests__/YouTubeWidget.test.tsx` is ≥ 1
    - `grep -c 'aria-label="YouTube feed, scrollable"\|"YouTube feed, scrollable"' components/widgets/__tests__/YouTubeWidget.test.tsx` is ≥ 1
    - `grep -c 'No videos available' components/widgets/__tests__/YouTubeWidget.test.tsx` is ≥ 1
    - `npx vitest run components/widgets/__tests__/YouTubeWidget.test.tsx` exits with code != 0 (RED)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire YouTubeWidget.tsx (GREEN)</name>
  <files>components/widgets/YouTubeWidget.tsx</files>
  <read_first>
    - components/widgets/YouTubeWidget.tsx (current 89 lines — confirm slice point at line 82 and WidgetCard call at line 63)
    - components/widgets/__tests__/YouTubeWidget.test.tsx (the tests just authored — the GREEN target)
    - lib/constants.ts (confirm `MAX_FEED_ITEMS` export is present per Plan 02)
  </read_first>
  <behavior>
    The rewired component must pass all five Task-1 tests and must NOT touch any file under `lib/api/`.
  </behavior>
  <action>
    Edit `components/widgets/YouTubeWidget.tsx`:

    1. Add `MAX_FEED_ITEMS` to the existing `@/lib/constants` import — there is currently no import from constants, so add a new line near the top: `import { MAX_FEED_ITEMS } from "@/lib/constants";`. Place it after the existing `@/lib/types` import.

    2. On the line `{videos.slice(0, 4).map((video) => (` (currently line 82), change `4` to `MAX_FEED_ITEMS`. No other change to the map body.

    3. On the populated-branch WidgetCard call (currently `<WidgetCard icon="▶" iconBg="var(--yt)" title="YouTube" badge={count > 0 ? `${count} new` : undefined} stale={stale}>`), add two new props **only** on the populated branch — which means the JSX must change so the WidgetCard opening tag is duplicated, OR a more idiomatic alternative: compute the new props inside the populated branch by passing them on a single WidgetCard. Simplest path: keep the single outer WidgetCard but pass `scrollable` + `maxBodyHeight` only when `videos` is a non-empty array. Use a ternary on those two props, e.g.:

       `<WidgetCard`
       `  icon="▶"`
       `  iconBg="var(--yt)"`
       `  title="YouTube"`
       `  badge={count > 0 ? `${count} new` : undefined}`
       `  stale={stale}`
       `  scrollable={Array.isArray(videos) && videos.length > 0 && !isLoading && !error ? true : undefined}`
       `  maxBodyHeight={Array.isArray(videos) && videos.length > 0 && !isLoading && !error ? "max-h-[320px]" : undefined}`
       `>`

       Rationale (matches UI-SPEC): the scrollable branch only activates on the populated path; loading/error/empty render under the non-scrollable branch. Passing `undefined` for both props is byte-identical to today's call for those branches.

    4. Do NOT modify anything inside `lib/api/youtube.ts`. Do NOT add a `Math.min` — `slice` already clamps.

    5. The badge expression `count > 0 ? `${count} new` : undefined` is preserved verbatim — D9 LOCKED.
  </action>
  <verify>
    <automated>npx vitest run components/widgets/__tests__/YouTubeWidget.test.tsx && npx tsc --noEmit && npm run lint -- --quiet</automated>
  </verify>
  <done>
    All 5 YouTubeWidget tests pass. `npx tsc --noEmit` clean. `npm run lint` clean. `git diff --stat lib/api/youtube.ts` shows zero lines changed (layer discipline preserved). The badge text continues to render as `${count} new` for `count` in 1..15.
  </done>
  <acceptance_criteria>
    - `npx vitest run components/widgets/__tests__/YouTubeWidget.test.tsx` exits 0
    - `grep -c 'videos.slice(0, MAX_FEED_ITEMS)' components/widgets/YouTubeWidget.tsx` returns 1
    - `grep -c 'videos.slice(0, 4)' components/widgets/YouTubeWidget.tsx` returns 0
    - `grep -cE 'import.*MAX_FEED_ITEMS.*@/lib/constants' components/widgets/YouTubeWidget.tsx` returns 1
    - `grep -c 'maxBodyHeight=' components/widgets/YouTubeWidget.tsx` returns 1
    - `grep -c 'scrollable=' components/widgets/YouTubeWidget.tsx` returns 1
    - `grep -c '`${count} new`' components/widgets/YouTubeWidget.tsx` returns 1 (D9 preserved)
    - `git diff --stat HEAD~..HEAD -- lib/api/youtube.ts` (run after commit) shows ` 0 files changed` OR the file is not listed
    - `npm test` exits 0
    - `npx tsc --noEmit` exits 0
    - `npm run lint` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new. Pure rendering change.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-supply-chain | Tampering | n/a | n/a | No new dependencies. |
</threat_model>

<verification>
- `npx vitest run components/widgets/__tests__/YouTubeWidget.test.tsx` exits 0 (5 tests pass)
- `lib/api/youtube.ts` byte-unchanged by this plan
- Full `npm test` suite green
- D1 + D9 LOCKED decisions implemented
</verification>

<success_criteria>
- 15-item cap via `MAX_FEED_ITEMS` constant (D1, D2)
- Scrollable WidgetCard wiring on populated branch (D3)
- Badge preservation (D9)
- Layer discipline: zero changes to `lib/api/youtube.ts` (CONTEXT.md "Out of in-scope artifacts")
</success_criteria>

<output>
After completion, create `.planning/phases/04-scrollable-feed-cards-scrum-49/04-04-SUMMARY.md` capturing:
- YouTubeWidget.tsx diff (slice line + WidgetCard call change)
- Test count (5 new)
- `git diff --stat lib/api/youtube.ts` confirmation (must be empty)
</output>
