---
phase: 04-scrollable-feed-cards-scrum-49
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vitest.config.ts
autonomous: true
requirements:
  - D10
  - SC-6
tags:
  - vitest
  - jsdom
  - testing-library
  - devDeps

must_haves:
  truths:
    - "npm test discovers .tsx test files (existing .ts files unaffected)"
    - "@testing-library/react v16, @testing-library/dom v10, and jsdom v29 are installed as devDependencies"
    - "npm audit --omit=dev reports no high/critical vulnerabilities from the three new packages"
    - "The existing 102 tests across 12 files still pass green after the install + glob change"
  artifacts:
    - path: "package.json"
      provides: "Three new devDependency entries"
      contains: "@testing-library/react"
    - path: "vitest.config.ts"
      provides: "Glob picks up .tsx test files"
      contains: "**/*.test.{ts,tsx}"
  key_links:
    - from: "vitest.config.ts"
      to: ".tsx test files"
      via: "include glob"
      pattern: "\\*\\*/\\*\\.test\\.\\{ts,tsx\\}"
---

<objective>
Wave 0 setup: install the three devDependencies required for jsdom-based component testing and extend the Vitest `include` glob to pick up `.tsx` test files. This unblocks every subsequent Wave 1 and Wave 2 plan whose verification depends on `*.test.tsx` files being discovered and run.

Purpose: Component tests for the scrollable WidgetCard branch (D3/D4/D5/D8) and per-widget slice caps (D1) cannot run under the current Vitest config — `include: ["**/*.test.ts"]` literally does not match `.test.tsx`, and there is no DOM environment to render React components into.

Output: Updated `package.json` (+ `package-lock.json`) carrying `@testing-library/react@^16`, `@testing-library/dom@^10`, `jsdom@^29` as devDependencies, plus a one-line change in `vitest.config.ts` extending the include glob.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-scrollable-feed-cards-scrum-49/04-CONTEXT.md
@.planning/phases/04-scrollable-feed-cards-scrum-49/04-RESEARCH.md
@.planning/phases/04-scrollable-feed-cards-scrum-49/04-VALIDATION.md
@CLAUDE.md
@vitest.config.ts
@package.json

<interfaces>
<!-- Vitest 4 environment strategy: per-file pragma. NO environmentMatchGlobs (removed in v4). -->
<!-- Vitest config currently: environment: "node", include: ["**/*.test.ts"], alias @ -> repo root. -->
<!-- Existing test count: 102 tests across 12 files. The glob extension must not break any of them. -->
<!-- Reference: 04-RESEARCH.md "Standard Stack" + "Pitfalls 1, 2, 3" + "Validation Architecture" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install three devDependencies for jsdom-based component tests</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current dev deps and scripts)
    - .planning/phases/04-scrollable-feed-cards-scrum-49/04-RESEARCH.md (sections "Standard Stack > New devDependencies" and "Pitfalls 2: @testing-library/dom peer missing")
  </read_first>
  <action>
    Run `npm install --save-dev @testing-library/react@^16 @testing-library/dom@^10 jsdom@^29` from the repo root. Install all three in one invocation so npm resolves the RTL v16 peer dependency on `@testing-library/dom` deterministically. Do NOT install `@testing-library/user-event` or `happy-dom` (per 04-RESEARCH "Optional > skip" and Open Question 4). After install, run `npm audit --omit=dev` and inspect output — any high/critical advisories from the three new packages must be surfaced (this is the only output of the T-04-supply-chain mitigation). Run `npm test` once to confirm the existing 102 tests still pass.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');const d=p.devDependencies||{};const need=['@testing-library/react','@testing-library/dom','jsdom'];const miss=need.filter(k=>!d[k]);if(miss.length){console.error('missing:',miss);process.exit(1)}console.log('all present',need.map(k=>k+':'+d[k]).join(','))"</automated>
  </verify>
  <done>
    `package.json` `devDependencies` contains entries for `@testing-library/react` (^16.x), `@testing-library/dom` (^10.x), and `jsdom` (^29.x). `package-lock.json` is updated. `npm test` still exits 0 with 102 tests passing. `npm audit --omit=dev` surfaces no new high/critical advisories attributable to the three new packages.
  </done>
  <acceptance_criteria>
    - `node -e "require('@testing-library/react')"` exits 0
    - `node -e "require('@testing-library/dom')"` exits 0
    - `node -e "require('jsdom')"` exits 0
    - `package.json` line `"@testing-library/react": "^16` (or higher 16.x) is present in `devDependencies`
    - `package.json` line `"@testing-library/dom": "^10` is present in `devDependencies`
    - `package.json` line `"jsdom": "^29` is present in `devDependencies`
    - `npm test` exits 0 (existing 102 tests still pass — install did not break the node-environment suite)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extend Vitest include glob to discover .tsx test files</name>
  <files>vitest.config.ts</files>
  <read_first>
    - vitest.config.ts (current `include: ["**/*.test.ts"]` line)
    - .planning/phases/04-scrollable-feed-cards-scrum-49/04-RESEARCH.md (Pitfall 3: "Forgetting to extend Vitest's include to pick up .tsx files" — quoted: `*.test.ts` is a literal pattern, does NOT match `.tsx`)
  </read_first>
  <action>
    In `vitest.config.ts`, change the `include` array from `["**/*.test.ts"]` to `["**/*.test.{ts,tsx}"]`. Do NOT touch `environment: "node"` — the jsdom environment is opted into per-file via the `// @vitest-environment jsdom` pragma (research Pattern 1; CONTEXT.md D10 with the research correction). Do NOT add `environmentMatchGlobs` — it was removed in Vitest 4 (research Pitfall 1, cited from `vitest.dev/guide/migration.html`). Leave the `exclude` array, `passWithNoTests`, and the `@` alias untouched.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('vitest.config.ts','utf8');if(!s.includes('\"**/*.test.{ts,tsx}\"')){console.error('include glob not updated');process.exit(1)};if(s.includes('environmentMatchGlobs')){console.error('environmentMatchGlobs is removed in vitest 4 — do not use');process.exit(1)};if(!/environment:\s*\"node\"/.test(s)){console.error('environment must remain node');process.exit(1)};console.log('vitest.config.ts ok')"</automated>
  </verify>
  <done>
    `vitest.config.ts` contains exactly `include: ["**/*.test.{ts,tsx}"]` (or equivalent array form covering both extensions). No `environmentMatchGlobs` is present. `environment: "node"` is unchanged. `npm test` exits 0 with the same 102 tests (no .tsx tests exist yet, so the count cannot increase here — that happens in plans 02–06).
  </done>
  <acceptance_criteria>
    - `grep -c '"\*\*/\*\.test\.{ts,tsx}"' vitest.config.ts` returns 1
    - `grep -c 'environmentMatchGlobs' vitest.config.ts` returns 0
    - `grep -c 'environment: "node"' vitest.config.ts` returns 1
    - `npm test` exits 0 (102 tests still pass; the include change does not break existing discovery)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local devDependencies | New package tarballs cross this boundary; tampering risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-supply-chain | Tampering | `@testing-library/react`, `@testing-library/dom`, `jsdom` install | mitigate | Pin to major versions (`^16`, `^10`, `^29`) verified against npm registry on 2026-05-14. Rely on existing `package-lock.json` to lock resolved versions. Run `npm audit --omit=dev` after install and surface any high/critical advisories. All three are widely-used, well-maintained packages with no current known supply-chain advisories (per RESEARCH.md "Security Domain"). They are devDependencies only — `next build` excludes them from production bundles. |
</threat_model>

<verification>
After both tasks:
- `npm test` exits 0 (102 existing tests)
- `vitest.config.ts` grep for `"**/*.test.{ts,tsx}"` returns 1 match
- `package.json` contains all three new devDependencies under `devDependencies`
- `npm audit --omit=dev` clean of new high/critical advisories
</verification>

<success_criteria>
- All three Wave 0 devDependencies installed and importable
- Vitest include glob picks up .tsx files
- Zero regression on the existing 102 tests
- T-04-supply-chain mitigated via version pin + audit
</success_criteria>

<output>
After completion, create `.planning/phases/04-scrollable-feed-cards-scrum-49/04-01-SUMMARY.md` listing:
- Installed versions (resolved from `package-lock.json`)
- `vitest.config.ts` diff
- `npm audit --omit=dev` outcome
- Existing-suite re-run result (test count, duration)
</output>
