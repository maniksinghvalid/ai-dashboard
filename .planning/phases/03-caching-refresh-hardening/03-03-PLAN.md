---
phase: 03-caching-refresh-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/api/sentiment.ts
  - lib/api/sentiment.test.ts
autonomous: true
requirements: [SC-3, SC-5]
must_haves:
  truths:
    - "The sentiment daily-budget check-and-consume is a single atomic operation, not a non-atomic get-then-incrby"
    - "Two concurrent runs cannot both pass the budget guard and together exceed SENTIMENT_DAILY_CHAR_BUDGET"
    - "A single consumer under budget still passes; the second concurrent consumer is rejected"
    - "The daily key format and the 48h expiry/reset semantics are unchanged"
    - "The 401 Sentry capture is untouched"
  artifacts:
    - path: "lib/api/sentiment.ts"
      provides: "atomic checkAndConsumeBudget"
      contains: "checkAndConsumeBudget"
    - path: "lib/api/sentiment.test.ts"
      provides: "atomic budget guard tests — single consumer passes, concurrent second consumer rejected"
  key_links:
    - from: "lib/api/sentiment.ts"
      to: "Redis"
      via: "single atomic eval (or incr-first-refund) replacing get + conditional incrby"
      pattern: "checkAndConsumeBudget"
---

<objective>
Replace the non-atomic daily-budget check-and-consume in `lib/api/sentiment.ts` with a single atomic operation so two concurrent runs cannot both pass the guard and together exceed `SENTIMENT_DAILY_CHAR_BUDGET` (P3 — High, SC-3).

Today `checkAndConsumeBudget` (`lib/api/sentiment.ts:76-85`) does `redis.get(key)` → `if (current + charsNeeded > budget) return false` → `redis.incrby(key, charsNeeded)` → `redis.expire(...)`. The gap between the `get` and the `incrby` is a double-spend window: two runs can both read the same `current`, both pass the check, both increment, and together overshoot the budget. Over the Upstash REST transport there is no MULTI/EXEC — atomicity must come from a single command or a single `eval`.

Purpose: the daily-char-budget circuit breaker (D1 / F1, a locked decision) actually holds under concurrency.
Output: `checkAndConsumeBudget` becomes a single atomic `redis.eval` Lua script (the recommended mechanism per 03-RESEARCH.md Open Question 3); the daily key format and 48h expiry are preserved; the 401 Sentry capture is untouched; co-located tests demonstrate the atomic guard rejects the second concurrent consumer.

This plan is fully independent of Plans 03-01, 03-02, 03-04 — it touches only `lib/api/sentiment.ts` and its test file. Wave 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-caching-refresh-hardening/03-CONTEXT.md
@.planning/phases/03-caching-refresh-hardening/03-RESEARCH.md
@.planning/phases/03-caching-refresh-hardening/03-PATTERNS.md

<interfaces>
<!-- Current non-atomic checkAndConsumeBudget (lib/api/sentiment.ts:76-85) -->
async function checkAndConsumeBudget(charsNeeded: number): Promise<boolean>
// current: redis.get<number>(key) ?? 0  →  if (current+charsNeeded > budget) return false  →  redis.incrby  →  redis.expire

<!-- @upstash/redis 1.37.0 eval (from 03-RESEARCH.md) -->
redis.eval(script: string, keys: string[], args: unknown[])
// Runs server-side atomically. Lua ARGV arrive as STRINGS — tonumber() before arithmetic.
// Lua `return 1` / `return 0` → JS number 1 / 0. Do NOT return a Lua boolean (false → null, true → 1).
// Branch on the JS side with === 1.

<!-- Preserve unchanged: todayKey() format (sentiment.ts:68-74), BUDGET_EXPIRE_SECONDS = 86_400 * 2 (line 12),
     DEFAULT_BUDGET = 200_000 (line 9), SENTIMENT_DAILY_CHAR_BUDGET env read, the 401 Sentry capture (lines 175-194) -->

<!-- Recommended Lua script (from 03-RESEARCH.md Pattern 2):
  local current = tonumber(redis.call("get", KEYS[1]) or "0")
  local needed = tonumber(ARGV[1])
  local budget = tonumber(ARGV[2])
  local ttl = tonumber(ARGV[3])
  if current + needed > budget then return 0 end
  redis.call("incrby", KEYS[1], needed)
  redis.call("expire", KEYS[1], ttl)
  return 1
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make checkAndConsumeBudget atomic via redis.eval</name>
  <files>lib/api/sentiment.ts</files>
  <read_first>
    - lib/api/sentiment.ts (full file — `checkAndConsumeBudget` at lines 76-85 is the target; `todayKey()` at 68-74; `BUDGET_EXPIRE_SECONDS` at line 12; `DEFAULT_BUDGET` at line 9; the single call site at line 143; the 401 Sentry capture at 175-194 — REGRESSION GUARD, do not touch)
    - .planning/phases/03-caching-refresh-hardening/03-RESEARCH.md (Pattern 2: atomic check-and-consume via Lua; Anti-Patterns — composing atomicity from multiple REST calls is the P3 bug, Lua ARGV string coercion, do not rely on Lua boolean → JS boolean; Standard Stack — redis.eval signature)
    - .planning/phases/03-caching-refresh-hardening/03-PATTERNS.md (`lib/api/sentiment.ts (MODIFIED)` section — what to preserve unchanged)
    - .planning/phases/03-caching-refresh-hardening/03-CONTEXT.md (P3 — Lua eval OR incr-first-refund are both acceptable; the contract is two concurrent runs cannot both pass)
  </read_first>
  <behavior>
    - checkAndConsumeBudget returns true when the eval resolves the integer 1 (consumed — under budget)
    - checkAndConsumeBudget returns false when the eval resolves the integer 0 (rejected — would exceed budget)
    - The Lua script is passed to redis.eval with [key] as the keys array and [charsNeeded, budget, BUDGET_EXPIRE_SECONDS] as the args array
  </behavior>
  <action>
    Rewrite the body of `checkAndConsumeBudget` to use a single atomic `redis.eval`. Keep the function signature `async function checkAndConsumeBudget(charsNeeded: number): Promise<boolean>` and keep the `budget` read (`Number(process.env.SENTIMENT_DAILY_CHAR_BUDGET ?? String(DEFAULT_BUDGET))`) and `const key = todayKey()` exactly as they are. Define a module-level `const` Lua script string that: reads `KEYS[1]` with a `tonumber(... or "0")` fallback, reads `tonumber(ARGV[1])` as needed / `tonumber(ARGV[2])` as budget / `tonumber(ARGV[3])` as ttl, returns `0` if `current + needed > budget`, otherwise does `redis.call("incrby", KEYS[1], needed)` then `redis.call("expire", KEYS[1], ttl)` then returns `1`. Every numeric ARGV MUST be wrapped in `tonumber()` — ARGV values arrive in Lua as strings. The script MUST `return` integer sentinels `1`/`0`, never a Lua boolean (Redis has no boolean reply; Lua `false` coerces to JS `null`). Call `const result = await redis.eval(SCRIPT, [key], [charsNeeded, budget, BUDGET_EXPIRE_SECONDS])` and `return result === 1`. Do NOT change: `todayKey()`, `BUDGET_EXPIRE_SECONDS`, `DEFAULT_BUDGET`, the `SENTIMENT_DAILY_CHAR_BUDGET` env var name/read, the single call site at line 143, or the 401 Sentry capture block (lines 175-194). The atomicity replaces the three-await `get`/`incrby`/`expire` sequence — there must be no remaining `redis.get` + conditional `redis.incrby` pair in `checkAndConsumeBudget`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `lib/api/sentiment.ts` `checkAndConsumeBudget` calls `redis.eval` with a Lua script string, `[key]` as keys, and `[charsNeeded, budget, BUDGET_EXPIRE_SECONDS]` as args
    - The Lua script contains `tonumber(` for each numeric ARGV and `return 1` / `return 0` (integer sentinels, not Lua booleans)
    - `checkAndConsumeBudget` no longer contains a `redis.get` followed by a conditional `redis.incrby` (the non-atomic pair is gone)
    - `lib/api/sentiment.ts` still contains `todayKey`, `BUDGET_EXPIRE_SECONDS`, and the 401 Sentry capture with `reason: "key-rotation-suspected"`
    - `npx tsc --noEmit` exits 0; `npm run lint` exits 0
  </acceptance_criteria>
  <done>checkAndConsumeBudget is a single atomic eval; daily key format, expiry, and 401 Sentry capture preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend sentiment.test.ts with atomic budget guard tests</name>
  <files>lib/api/sentiment.test.ts</files>
  <read_first>
    - lib/api/sentiment.test.ts (current file — note per 03-RESEARCH.md Wave 0 Gaps it currently mocks NOTHING; the existing pure-function tests for `preprocessText` / `aggregateSentiment` stay untouched — this task ADDS a `vi.mock` block and new cases)
    - lib/api/sentiment.ts (the atomic `checkAndConsumeBudget` from Task 1 — to know what to mock)
    - lib/cache/timeseries.test.ts (canonical boundary-mock pattern — `vi.mock("@/lib/cache/redis")`, `mockRedis`, `beforeEach` reset)
    - lib/api/alerts.test.ts (the multi-mock pattern, in case a sibling module also needs stubbing)
    - .planning/phases/03-caching-refresh-hardening/03-RESEARCH.md (Code Examples — P3 atomic budget test cases: mock eval's integer sentinel return)
  </read_first>
  <behavior>
    - A single consumer under budget passes: eval resolves 1 → checkAndConsumeBudget resolves true
    - The second concurrent consumer is rejected: eval resolves 0 → checkAndConsumeBudget resolves false
    - Demonstrate the concurrent scenario: two consumers near the limit, the first eval resolves 1 (passes) and the second eval resolves 0 (rejected) — total consumption stays at or under SENTIMENT_DAILY_CHAR_BUDGET
  </behavior>
  <action>
    Extend `lib/api/sentiment.test.ts`. Add the Redis boundary-mock block at the top following `timeseries.test.ts`: `vi.mock("@/lib/cache/redis", () => ({ getRedis: vi.fn() }))`, a `mockRedis` object with at least `eval: vi.fn()`, and a `beforeEach` that clears mocks and wires `vi.mocked(getRedis).mockReturnValue(mockRedis as any)`. Leave the existing `preprocessText` / `aggregateSentiment` pure-function tests exactly as-is — they do not use Redis.
    `checkAndConsumeBudget` is not exported (it is module-internal). To test it, exercise it through its single caller `fetchAndCacheSentiment` (the call site at `sentiment.ts:143` — `if (!(await checkAndConsumeBudget(totalChars))) throw`): when `mockRedis.eval` resolves `0`, calling `fetchAndCacheSentiment` with a non-empty item list rejects/throws with the `daily char budget tripped` error; when `mockRedis.eval` resolves `1`, the budget guard passes (the test can then stub `fetch` or let it fail later — the assertion is only that the budget guard did not throw the budget error). If exercising through `fetchAndCacheSentiment` proves to require too much `fetch` stubbing, the executor may instead add a narrow `export` of `checkAndConsumeBudget` for testability — but prefer the through-the-caller approach to keep the module surface minimal. Cover the three behaviors above. For the concurrent scenario, use `mockRedis.eval.mockResolvedValueOnce(1).mockResolvedValueOnce(0)` to model the atomic guard letting exactly one consumer through.
  </action>
  <verify>
    <automated>npx vitest run lib/api/sentiment.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/api/sentiment.test.ts` contains `vi.mock("@/lib/cache/redis"` and a `mockRedis` with an `eval` mock
    - The existing `preprocessText` / `aggregateSentiment` tests are still present and still pass
    - There is a test where `mockRedis.eval` resolves `0` and the budget guard rejects (the `daily char budget tripped` path)
    - There is a test where `mockRedis.eval` resolves `1` and the budget guard passes
    - There is a test modelling two concurrent consumers (`mockResolvedValueOnce(1)` then `mockResolvedValueOnce(0)`) demonstrating only the first passes
    - `npx vitest run lib/api/sentiment.test.ts` exits 0
  </acceptance_criteria>
  <done>The atomic budget guard is proven: single consumer passes, second concurrent consumer rejected; existing sentiment tests unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron Tier 3 → sentiment module | In-process call; `charsNeeded` is computed from already-fetched feed text, not raw attacker input. |
| sentiment module → Redis | The daily-budget counter key `sentiment:budget:<UTC-date>` and the new Lua `eval`. An attacker with direct Redis access could manipulate the counter. |
| sentiment module → Together AI | External API. Unchanged by P3 — the 401 Sentry capture is a regression guard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-08 | Tampering / Injection | The Lua `eval` script | mitigate | The Lua script is a fixed module-level string constant — it is NEVER built by string-concatenating runtime values. The dynamic values (`charsNeeded`, `budget`, `BUDGET_EXPIRE_SECONDS`) are passed exclusively as `EVAL` `ARGV` parameters, not interpolated into the script body. This makes Lua injection structurally impossible. Enforced by acceptance criterion: the script is passed as a constant with `[charsNeeded, budget, BUDGET_EXPIRE_SECONDS]` as the args array. |
| T-3-09 | Tampering | The `sentiment:budget:<UTC-date>` Redis counter | accept | An attacker with direct Redis write access could zero the counter (defeating the budget cap) or inflate it (DoS-ing sentiment). This requires already-compromised Redis credentials — a separate, higher-tier concern; the budget counter is not the weak link once Redis itself is owned. The 48h key expiry bounds any malicious inflation to two days. |
| T-3-10 | Elevation of privilege (quota bypass) | concurrent double-spend of the char budget | mitigate | This is the exact bug P3 fixes. The single atomic `eval` (server-side check-then-incr in one Redis operation) closes the get/incrby race window so two concurrent runs cannot both pass. Verified by the concurrent-consumer test (eval resolves 1 then 0). |
| T-3-11 | Information disclosure / Spoofing / Repudiation / DoS | — | N/A | P3 introduces no new endpoint, no new external input surface, no new auth path. It is an internal atomicity refactor of an existing module-private function. |
</threat_model>

<verification>
- `npx vitest run lib/api/sentiment.test.ts` — atomic budget guard tests + existing pure-function tests green
- `npm test` — full suite still green, no regressions
- `npx tsc --noEmit && npm run lint` — clean
- Manual reasoning check: with the atomic `eval`, two concurrent `checkAndConsumeBudget` calls near the limit serialize server-side in Redis — the first `eval` does check-then-incr atomically, the second `eval` sees the incremented counter and returns `0`. Total consumption ≤ `SENTIMENT_DAILY_CHAR_BUDGET`.
</verification>

<success_criteria>
- SC-3 (P3) satisfied: the sentiment daily-budget check-and-consume is a single atomic operation; two concurrent runs cannot both pass; the second concurrent consumer is rejected; a unit test demonstrates the atomic guard rejects the second concurrent consumer.
- SC-5 (partial): `lib/api/sentiment.test.ts` passes (new + existing cases); `npm test && npx tsc --noEmit && npm run lint` clean.
- Regression guards held: `todayKey()` format, `BUDGET_EXPIRE_SECONDS` 48h expiry, `SENTIMENT_DAILY_CHAR_BUDGET` env contract, and the 401 Sentry capture all unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/03-caching-refresh-hardening/03-03-SUMMARY.md`
</output>
